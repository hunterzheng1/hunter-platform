{
  "name": "hunter-platform",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "overrides": {
    "postcss": "8.5.23"
  },
  "scripts": {
    "build": "node node_modules/typescript/bin/tsc -b && npm run build -w apps/web",
    "typecheck": "node node_modules/typescript/bin/tsc -b && npm run typecheck -w apps/web",
    "test": "node node_modules/vitest/vitest.mjs run --maxWorkers=2",
    "test:watch": "node node_modules/vitest/vitest.mjs",
    "lint": "node node_modules/eslint/bin/eslint.js .",
    "dev:server": "npm run build -w packages/contracts -w packages/core -w apps/server && npm run start -w apps/server",
    "dev:web": "npm run dev -w apps/web",
    "check:push": "npm run lint && npm run typecheck",
    "check": "npm run lint && npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.0.0",
    "eslint": "10.5.0",
    "tsx": "4.22.4",
    "typescript": "6.0.3",
    "typescript-eslint": "8.61.1",
    "vitest": "4.1.9"
  }
}
