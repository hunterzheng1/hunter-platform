openapi: 3.1.0
info:
  title: Hunter Harness API
  version: 1.1.0
servers:
  - url: https://harness.example.com
security:
  - bearerAuth: []
paths:
  /health:
    get:
      operationId: health
      security: []
      responses:
        "200":
          description: Process liveness
  /api/v1/dashboard/overview:
    get:
      operationId: getDashboardOverview
      parameters:
        - name: days
          in: query
          schema:
            type: integer
            minimum: 7
            maximum: 30
            default: 7
      responses:
        "200":
          description: Authenticated governance metrics, trend, health checks, service reads, and sanitized audit activity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardOverview"
        default:
          $ref: "#/components/responses/Error"
  /api/v1/projects:resolve:
    post:
      operationId: resolveProject
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectResolveRequest"
      responses:
        "200":
          description: Project created or bound
        default:
          $ref: "#/components/responses/Error"
  /api/v1/projects:
    get:
      operationId: listProjects
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Project registry page
        default:
          $ref: "#/components/responses/Error"
  /api/v1/projects/{project_id}:
    get:
      operationId: getProject
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Project metadata
        default:
          $ref: "#/components/responses/Error"
  /api/v1/projects/{project_id}/proposal-sessions:
    post:
      operationId: createProposalSession
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201":
          description: Upload session created
        default:
          $ref: "#/components/responses/Error"
  /api/v1/proposal-sessions/{session_id}/blobs:query:
    post:
      operationId: queryBlobs
      parameters:
        - $ref: "#/components/parameters/SessionId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: Present and missing blob hashes
        default:
          $ref: "#/components/responses/Error"
  /api/v1/proposal-sessions/{session_id}/blobs/{content_sha256}:
    put:
      operationId: uploadBlob
      parameters:
        - $ref: "#/components/parameters/SessionId"
        - $ref: "#/components/parameters/ContentSha256"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "202":
          description: Chunk accepted
        default:
          $ref: "#/components/responses/Error"
  /api/v1/proposal-sessions/{session_id}:finalize:
    post:
      operationId: finalizeProposal
      parameters:
        - $ref: "#/components/parameters/SessionId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinalizeProposalRequest"
      responses:
        "201":
          description: Proposal auto-approved and artifact published
        "409":
          description: Server already has a newer artifact for this project; re-sync base_artifact_id before finalizing again (STALE_PUSH)
        "422":
          description: Sensitive content scan blocked the proposal (SENSITIVE_CONTENT_BLOCKED); error.details may include finding_count, scanner_version, and findings summary
        default:
          $ref: "#/components/responses/Error"
  /api/v1/projects/{project_id}/proposals:
    get:
      operationId: listProposals
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Proposal page
        default:
          $ref: "#/components/responses/Error"
  /api/v1/projects/{project_id}/artifacts:
    get:
      operationId: listArtifacts
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Approved artifact history page
        default:
          $ref: "#/components/responses/Error"
  /api/v1/proposals/{proposal_id}:
    get:
      operationId: getProposal
      parameters:
        - $ref: "#/components/parameters/ProposalId"
      responses:
        "200":
          description: Proposal details
        default:
          $ref: "#/components/responses/Error"
  /api/v1/projects/{project_id}/update-manifest:
    get:
      operationId: getUpdateManifest
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Approved artifact delta metadata
        default:
          $ref: "#/components/responses/Error"
  /api/v1/artifacts/{artifact_id}/manifest:
    get:
      operationId: getArtifactManifest
      parameters:
        - $ref: "#/components/parameters/ArtifactId"
      responses:
        "200":
          description: Immutable artifact manifest
        default:
          $ref: "#/components/responses/Error"
  /api/v1/artifacts/{artifact_id}/blobs/{content_sha256}:
    get:
      operationId: downloadArtifactBlob
      parameters:
        - $ref: "#/components/parameters/ArtifactId"
        - $ref: "#/components/parameters/ContentSha256"
      responses:
        "200":
          description: Artifact blob
        default:
          $ref: "#/components/responses/Error"
  /api/v1/skills:
    get:
      operationId: listSkills
      responses:
        "200": { description: Canonical Skill Registry }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skill-catalog/order:
    get:
      operationId: getSkillCatalogOrder
      responses:
        "200":
          {
            description: Persisted manual ordering for local and external skill cards,
          }
        default: { $ref: "#/components/responses/Error" }
    put:
      operationId: updateSkillCatalogOrder
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [items, revision]
              properties:
                items:
                  type: array
                  maxItems: 10000
                  uniqueItems: true
                  items:
                    {
                      type: string,
                      pattern: "^(registry|external):[A-Za-z0-9][A-Za-z0-9._-]*$",
                    }
                revision: { type: integer, minimum: 0 }
      responses:
        "200": { description: Skill card order saved }
        "409": { description: Skill card order revision conflict }
        "422": { description: Order contains an unknown skill }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}:
    get:
      operationId: getSkill
      responses:
        "200": { description: Canonical Skill detail }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: deleteSkill
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          { description: Skill and its draft, versions, and artifacts deleted }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/versions:
    get:
      operationId: listSkillVersions
      parameters:
        - name: agent
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/RegistryAgent"
          description: Filter versions by agent; when omitted all versions are returned. An invalid agent returns 422 VALIDATION_FAILED.
      responses:
        "200":
          {
            description: Published Skill versions,
            optionally filtered by agent,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/adapter-preview/{agent}:
    get:
      operationId: previewSkillAdapter
      responses:
        "200":
          {
            description: Verified published adapter output preview for any installable adapter (claude-code/codex/cursor/generic); mcp returns 422 ADAPTER_NOT_IMPLEMENTED,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/artifacts/{agent}/download:
    get:
      operationId: downloadSkillArtifact
      responses:
        "200": { description: Approved adapter ZIP with SHA-256 headers }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/draft:
    post:
      operationId: uploadSkillDraft
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: agent
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/SkillTargetAgent"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: array
                  items: { type: string, format: binary }
                sensitive_review:
                  $ref: "#/components/schemas/SensitiveReviewSubmission"
      responses:
        "201": { description: Skill source variant draft created or updated }
        "409":
          {
            description: Review fingerprints or scanner version are stale (SENSITIVE_REVIEW_STALE),
          }
        "413":
          {
            description: Upload or review payload exceeds server limits (SKILL_UPLOAD_TOO_LARGE),
          }
        "422":
          {
            description: Bundle invalid,
            high-risk content blocked,
            or explicit sensitive review required,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}:
    get:
      operationId: getSkillDraft
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
      responses:
        "200":
          {
            description: Unpublished per-agent Skill draft with source files and checks,
          }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: discardSkillDraft
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Per-agent Skill draft discarded }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/checks:
    post:
      operationId: runSkillDraftChecks
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Programmatic per-agent Skill draft check results }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/ai-checks:
    post:
      operationId: runSkillDraftAiChecks
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: "Starts async AI check job; returns jobId with status pending. Poll GET /api/v1/ai-jobs/{jobId} for result. 429 QUOTA_EXCEEDED if daily limit reached",
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/fix-preview:
    post:
      operationId: previewSkillDraftFix
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
      responses:
        "200": { description: Fix plan preview without persisting }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/apply-fix:
    post:
      operationId: applySkillDraftFix
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Updated per-agent skill draft after applying fix }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/release-note:generate:
    post:
      operationId: generateSkillDraftReleaseNote
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: AI-generated release note persisted to per-agent draft.releaseNote (degraded on AI failure),
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/fix-suggestions:
    post:
      operationId: fetchSkillDraftFixSuggestions
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
      responses:
        "200":
          {
            description: AI-generated fix suggestions as a read-only FixPlan preview,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/apply-fix-suggestion:
    post:
      operationId: applySkillDraftFixSuggestion
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: Updated per-agent skill draft after applying an AI fix suggestion,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/publish:
    post:
      operationId: publishSkillDraft
      deprecated: true
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: Per-agent Skill draft promoted to a new published version with one artifact for the specified agent,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/publish:
    post:
      operationId: publishUnifiedSkill
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublishUnifiedSkillRequest"
      responses:
        "200":
          description: Registry release and scoped public npm publication completed atomically from the caller perspective
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublishSkillResponse"
        "409":
          {
            description: Draft revision/version conflict or remote package digest conflict,
          }
        "422":
          { description: Sensitive content or target variant validation failed }
        "502":
          {
            description: npm registry rejected or failed publication (NPM_PUBLISH_FAILED),
          }
        "503":
          {
            description: npm scope or token secret is not configured (NPM_PUBLISH_NOT_CONFIGURED),
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/npm-release:
    post:
      operationId: releaseSkillToNpm
      deprecated: true
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: Latest published skill version released to npm as a data-only package,
          }
        "503":
          {
            description: npm publish is not configured on the server (NPM_PUBLISH_NOT_CONFIGURED),
          }
        "502":
          {
            description: npm registry rejected publication (NPM_PUBLISH_FAILED),
          }
        "409":
          {
            description: npm registry already has this package version (NPM_PUBLISH_CONFLICT),
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/draft/{agent}/diff:
    get:
      operationId: diffSkillDraft
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/Agent"
      responses:
        "200": { description: Published vs per-agent draft source file diff }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/default-agent:
    patch:
      operationId: setSkillDefaultAgent
      parameters:
        - $ref: "#/components/parameters/SkillSlug"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetDefaultAgentRequest"
      responses:
        "200":
          { description: Skill default agent switched with optimistic revision }
        "401": { description: Auth required }
        "422":
          {
            description: Agent not enabled for this skill (AGENT_NOT_ENABLED) or request body validation failed,
          }
        "409": { description: Skill revision conflict (REVISION_CONFLICT) }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/system/npm-publishing:
    get:
      operationId: getNpmPublishingCredentialStatus
      summary: Read the server-wide npm publishing credential status
      description: Owner session only. The response never contains the credential token or encrypted envelope.
      responses:
        "200":
          description: Active managed or deployment credential metadata
          content:
            application/json:
              schema:
                { $ref: "#/components/schemas/NpmPublishingCredentialStatus" }
        "403": { description: A human Owner session is required }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/system/npm-publishing/credential:
    put:
      operationId: replaceNpmPublishingCredential
      summary: Verify and atomically replace the managed npm credential
      description: Owner session only. The write-only token is verified with npm before its encrypted envelope replaces the active managed credential.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              {
                $ref: "#/components/schemas/NpmPublishingCredentialReplaceRequest",
              }
      responses:
        "200":
          description: Managed credential verified and encrypted
          content:
            application/json:
              schema:
                { $ref: "#/components/schemas/NpmPublishingCredentialStatus" }
        "403": { description: A human Owner session is required }
        "422":
          {
            description: npm rejected the credential or the supplied expiration has passed,
          }
        "503":
          { description: The server credential-encryption key is unavailable }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: removeNpmPublishingCredential
      summary: Remove the managed credential and fall back to the deployment secret
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SchemaVersionRequest" }
      responses:
        "200":
          description: Managed credential removed; response describes the active fallback
          content:
            application/json:
              schema:
                { $ref: "#/components/schemas/NpmPublishingCredentialStatus" }
        "403": { description: A human Owner session is required }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/system/npm-publishing/verify:
    post:
      operationId: verifyNpmPublishingCredential
      summary: Verify the active credential against npm
      description: Owner session only. Managed credentials are decrypted only in memory for the verification request.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SchemaVersionRequest" }
      responses:
        "200":
          description: Active credential identity verified
          content:
            application/json:
              schema:
                { $ref: "#/components/schemas/NpmPublishingCredentialStatus" }
        "403": { description: A human Owner session is required }
        "422":
          { description: npm rejected the active credential or it has expired }
        "503": { description: The managed credential cannot be decrypted }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/providers:
    get:
      operationId: listAiProviders
      responses:
        "200":
          {
            description: API-key provider configurations without secrets; Codex account authorization is managed by its independent endpoint,
          }
        default: { $ref: "#/components/responses/Error" }
    post:
      operationId: createAiProvider
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201": { description: API-key provider created }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/providers/{provider_id}:
    patch:
      operationId: updateAiProvider
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: API-key provider updated }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: deleteAiProvider
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: AI provider deleted }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/providers/{provider_id}/test:
    post:
      operationId: testAiProvider
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: AI provider connectivity test result }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/codex:
    get:
      operationId: getCodexConnection
      responses:
        "200":
          description: ChatGPT account connection and the models currently visible to Codex; never contains tokens
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CodexConnectionState" }
        default: { $ref: "#/components/responses/Error" }
    patch:
      operationId: updateCodexConnection
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [schema_version]
              anyOf:
                - required: [selected_model]
                - required: [enabled]
              properties:
                schema_version: { const: 1 }
                selected_model: { type: [string, "null"] }
                enabled:
                  type: boolean
                  description: Enables Codex as the single platform-wide model source; enabling it disables every API provider
      responses:
        "200":
          description: Codex default model or platform-wide enabled state updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CodexConnectionState" }
        "409": { description: ChatGPT account is not connected }
        "422": { description: Model is not available for this account }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: disconnectCodex
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: ChatGPT account disconnected and Codex model preference cleared,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/codex/login:
    post:
      operationId: startCodexLogin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [schema_version]
              properties:
                schema_version: { const: 1 }
      responses:
        "200":
          description: Official ChatGPT device-code authorization instructions
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CodexLoginStart" }
        "503": { description: Codex App Server is unavailable }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/codex/login/cancel:
    post:
      operationId: cancelCodexLogin
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [schema_version, login_id]
              properties:
                schema_version: { const: 1 }
                login_id: { type: string, minLength: 1 }
      responses:
        "200":
          { description: Pending ChatGPT device-code authorization cancelled }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/codex/test:
    post:
      operationId: testCodexConnection
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [schema_version]
              properties:
                schema_version: { const: 1 }
      responses:
        "200":
          { description: Codex account and selected-model connectivity result }
        "409": { description: ChatGPT account is not connected }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-config/usage:
    get:
      operationId: getAiUsage
      responses:
        "200":
          {
            description: Per-provider per-day AI quota usage statistics (UTC date roll),
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/ai-jobs/{jobId}:
    get:
      operationId: getAiJob
      parameters:
        - name: jobId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Async AI job state (pending/running/completed/failed); completed carries SkillCheckResult in result, failed carries error message. slug+agent are dedup key (R2).
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId: { type: string }
                  slug: { type: string, description: dedup key part 1 }
                  agent:
                    {
                      type: string,
                      enum: [claude-code, codex, cursor, generic, mcp],
                    }
                  status:
                    {
                      type: string,
                      enum: [pending, running, completed, failed],
                    }
                  result: { description: SkillCheckResult or null }
                  error: { type: string, nullable: true }
                  createdAt: { type: string, format: date-time }
                  expiresAt: { type: string, format: date-time }
        "404": { description: Job not found or expired (JOB_NOT_FOUND) }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skill-artifacts:
    get:
      operationId: listSkillArtifacts
      responses:
        "200": { description: Published Skill adapter artifacts }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/tags:
    get:
      operationId: listSkillTags
      responses:
        "200": { description: Skill tags }
        default: { $ref: "#/components/responses/Error" }
    post:
      operationId: createSkillTag
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201": { description: Tag created directly with audit }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/tags/{tag_id}:
    patch:
      operationId: updateSkillTag
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Tag renamed or deactivated }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/tags/{tag_id}/merge:
    post:
      operationId: mergeSkillTag
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Tag bindings merged directly with audit }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/skills/{slug}/tags/{tag_id}:
    put:
      operationId: bindSkillTag
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Tag bound to Skill }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: unbindSkillTag
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Tag removed from Skill }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/agent-tools:
    get:
      operationId: listAgentTools
      responses:
        "200":
          description: Registered Agent tools, including exact source references and workflow relationships
          content:
            application/json:
              schema:
                type: object
                required: [items, request_id]
                properties:
                  items:
                    type: array
                    items: { $ref: "#/components/schemas/AgentTool" }
                  request_id: { type: string, format: uuid }
        default: { $ref: "#/components/responses/Error" }
    post:
      operationId: createAgentTool
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateAgentToolRequest" }
      responses:
        "201":
          description: Agent tool registered
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AgentTool" }
        "409":
          { description: Agent tool slug already exists (AGENT_TOOL_EXISTS) }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/agent-tools/{slug}:
    get:
      operationId: getAgentTool
      parameters:
        - name: slug
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Agent tool detail with its exact npm, GitHub, or website source
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AgentTool" }
        "404": { description: Agent tool not found (AGENT_TOOL_NOT_FOUND) }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/agent-tools/import/inspect:
    post:
      operationId: inspectAgentToolGithub
      description: Read a GitHub repository and suggest Agent catalog fields without changing registry state.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/InspectAgentToolGithubRequest" }
      responses:
        "200":
          description: GitHub repository metadata and suggested Agent registration
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AgentToolGithubInspection" }
        "404":
          {
            description: GitHub repository not found (EXTERNAL_SOURCE_NOT_FOUND),
          }
        "502": { description: GitHub request failed }
        "504": { description: GitHub request timed out }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/agent-tools/import/ai-prefill:
    post:
      operationId: generateAgentToolPrefill
      description: Use the configured default AI source to create an editable Chinese Agent registration draft from inspected repository metadata.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/GenerateAgentToolPrefillRequest" }
      responses:
        "200":
          description: Editable Agent registration draft with a Simplified Chinese description
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AgentToolSuggestion" }
        "422": { description: AI source is not configured }
        "502":
          { description: AI generation or structured output parsing failed }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families:
    get:
      operationId: listWorkflowFamilies
      responses:
        "200": { description: Workflow families }
        default: { $ref: "#/components/responses/Error" }
    post:
      operationId: createWorkflowFamily
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201": { description: Workflow family created }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/import/inspect:
    post:
      operationId: inspectWorkflowFamilySource
      description: Resolve an npm package or exact GitHub tree path, detect its workflow-family manifest, profiles, and remote version without changing registry state.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              {
                $ref: "#/components/schemas/InspectWorkflowFamilySourceRequest",
              }
      responses:
        "200":
          description: Source inspection and import readiness
          content:
            application/json:
              schema:
                { $ref: "#/components/schemas/WorkflowFamilySourceInspection" }
        "404":
          {
            description: npm package or GitHub repository not found (EXTERNAL_SOURCE_NOT_FOUND),
          }
        "422":
          {
            description: GitHub subpath missing or no workflow profiles detected,
          }
        "502": { description: Upstream npm or GitHub request failed }
        "504": { description: Upstream npm or GitHub request timed out }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/import:
    post:
      operationId: importWorkflowFamilySource
      description: Import the inspected source as a new workflow family and stage every detected profile as one unpublished draft.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/ImportWorkflowFamilySourceRequest" }
      responses:
        "201":
          description: Workflow family created with a reviewable profile draft
          content:
            application/json:
              schema:
                {
                  $ref: "#/components/schemas/WorkflowFamilySourceImportResult",
                }
        "409":
          {
            description: "Workflow family slug already exists, or source content changed after preflight (WORKFLOW_SOURCE_CHANGED)",
          }
        "422":
          {
            description: Source profiles are incomplete or the GitHub subpath is invalid,
          }
        "502": { description: Upstream npm or GitHub request failed }
        "504": { description: Upstream npm or GitHub request timed out }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}:
    get:
      operationId: getWorkflowFamily
      responses:
        "200": { description: Workflow family detail }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/draft/profiles/{profile}:
    post:
      operationId: uploadWorkflowFamilyProfileDraft
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201":
          { description: Per-profile workflow family draft uploaded from ZIP }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/draft:
    get:
      operationId: getWorkflowFamilyDraft
      responses:
        "200":
          description: Lightweight unpublished workflow family draft metadata
          content:
            application/json:
              schema:
                {
                  $ref: "#/components/schemas/WorkflowFamilySourceImportResult/properties/draft",
                }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: discardWorkflowFamilyDraft
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Workflow family draft discarded }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/draft/checks:
    post:
      operationId: runWorkflowFamilyChecks
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200": { description: Workflow family draft check results }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/publish:
    post:
      operationId: publishWorkflowFamily
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: Published workflow family version metadata covering all required profiles
          content:
            application/json:
              schema:
                { $ref: "#/components/schemas/WorkflowFamilyVersionSummary" }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/draft/diff:
    get:
      operationId: diffWorkflowFamilyDraft
      responses:
        "200":
          {
            description: Diff files between published and draft workflow family profile bundle,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/versions:
    get:
      operationId: listWorkflowFamilyVersions
      responses:
        "200":
          description: Published workflow family version metadata sequence
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [items, request_id]
                properties:
                  items:
                    type: array
                    items:
                      {
                        $ref: "#/components/schemas/WorkflowFamilyVersionSummary",
                      }
                  request_id: { type: string, format: uuid }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/sync:
    post:
      operationId: syncWorkflowFamilyFromSource
      description: Check the registered npm or GitHub source and stage newer required profiles as a draft.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: Source checked; updated indicates whether a draft was staged
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WorkflowFamilySyncResult" }
        "409":
          {
            description: "Source content changed without a version bump (WORKFLOW_SOURCE_VERSION_CONFLICT), or the family changed during resolution (WORKFLOW_FAMILY_CHANGED)",
          }
        "422":
          {
            description: Family has no source or the source lacks required profile bundles,
          }
        "502": { description: Upstream npm or GitHub request failed }
        "504": { description: Upstream npm or GitHub request timed out }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/npm-release:
    post:
      operationId: releaseWorkflowFamilyToNpm
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: Latest published workflow family version released to npm as a data-only package,
          }
        "503":
          {
            description: npm publish is not configured on the server (NPM_PUBLISH_NOT_CONFIGURED),
          }
        "409":
          {
            description: npm registry already has this package version (NPM_PUBLISH_CONFLICT),
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/workflow-families/{slug}/artifacts/{profile}/download:
    get:
      operationId: downloadWorkflowFamilyProfileArtifact
      responses:
        "200":
          {
            description: Download published per-profile workflow family ZIP artifact,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/external-skills:
    get:
      operationId: listExternalSkills
      parameters:
        - name: search
          in: query
          schema: { type: string }
        - name: source_type
          in: query
          schema: { type: string, enum: [npm, github] }
      responses:
        "200":
          {
            description: Curated external skill listings (display-only metadata snapshots),
          }
        default: { $ref: "#/components/responses/Error" }
    post:
      operationId: createExternalSkill
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201":
          {
            description: External skill registered after fetching upstream metadata,
          }
        "404":
          { description: Upstream source not found (EXTERNAL_SOURCE_NOT_FOUND) }
        "409":
          {
            description: Same npm/GitHub source already registered (EXTERNAL_SKILL_EXISTS),
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/external-skills/{id}:
    get:
      operationId: getExternalSkill
      responses:
        "200":
          {
            description: External skill detail including snapshot,
            versioned AI summary,
            tags,
            and per-release update history,
          }
        "404": { description: External skill not found }
        default: { $ref: "#/components/responses/Error" }
    patch:
      operationId: patchExternalSkill
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: External skill tags updated; legacy curationNote remains accepted for compatibility,
          }
        "409": { description: Revision conflict }
        default: { $ref: "#/components/responses/Error" }
    delete:
      operationId: deleteExternalSkill
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          { description: External skill listing removed (upstream untouched) }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/external-skills/{id}/refresh:
    post:
      operationId: refreshExternalSkill
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: Confirmed upstream update applied; existing AI summary is preserved and every upstream release crossed by the update is recorded when available,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/external-skills/{id}/check-upstream:
    post:
      operationId: checkExternalSkillUpstream
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: Upstream checked without replacing the active snapshot or changing stored update notes,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/external-skills/{id}/update-history/refresh:
    post:
      operationId: refreshExternalSkillUpdateHistory
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [applied_at]
              properties:
                applied_at: { type: string, format: date-time }
              additionalProperties: false
      responses:
        "200":
          {
            description: Release notes for one stored update record refreshed from upstream,
          }
        "404": { description: External skill or update record not found }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/external-skills/{id}/summary:
    post:
      operationId: generateExternalSkillSummary
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [revision]
              properties:
                revision: { type: integer, minimum: 1 }
                force: { type: boolean, default: false }
              additionalProperties: false
      responses:
        "200":
          {
            description: Cached or newly generated structured Chinese AI summary,
          }
        "409":
          {
            description: Revision conflict or upstream content changed during generation,
          }
        "422":
          {
            description: Neither a default API provider nor a connected Codex account is available,
          }
        "502":
          { description: AI generation or structured response parsing failed }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/workflow-binding:
    get:
      operationId: getProjectWorkflowBinding
      responses:
        "200":
          {
            description: Current project workflow family binding (family + profile),
          }
        default: { $ref: "#/components/responses/Error" }
    put:
      operationId: bindProjectWorkflowFamily
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          {
            description: Project workflow family binding updated with optimistic revision,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/instruction-proposals:
    post:
      operationId: createInstructionProposal
      summary: Audit project guidance and generate a Chinese proposal without applying it
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InstructionProposalRequest"
      responses:
        "201":
          description: Marker-free Chinese instruction/rule proposal; rule candidates are never auto-applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstructionProposal"
        "422":
          { description: Evidence hash mismatch or sensitive content blocked }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/changes/{change_key}/archive-package:
    put:
      operationId: uploadChangeArchivePackage
      summary: Store one verified core-only ZIP, publish its files, and ingest knowledge server-side
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/ChangeKey"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/zip:
            schema: { type: string, format: binary }
      responses:
        "201":
          description: Original ZIP durably stored; receipt includes knowledge indexing status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArchivePackageReceipt"
        "413":
          { description: Archive package exceeds the configured upload limit }
        "409":
          {
            description: A different immutable ZIP already exists for this change,
          }
        "415": { description: Request body is not an application/zip buffer }
        "422":
          {
            description: Unsafe ZIP,
            non-core file,
            manifest/hash mismatch,
            or sensitive content,
          }
        default: { $ref: "#/components/responses/Error" }
    get:
      operationId: getChangeArchivePackage
      summary: Get durable archive and server-side knowledge status
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/ChangeKey"
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Current archive package receipt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArchivePackageReceipt"
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/changes/{change_key}/archive-package/download:
    get:
      operationId: downloadChangeArchivePackage
      summary: Recover the exact original ZIP stored for a change
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/ChangeKey"
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Original immutable ZIP; X-Content-SHA256 carries its package hash
          headers:
            X-Content-SHA256:
              schema: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
          content:
            application/zip:
              schema: { type: string, format: binary }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/branches/{branch_name}/remote-sync/content-upload:
    post:
      operationId: stageRemoteContentUpload
      summary: Stage one bounded raw ZIP stream for remote archive processing
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: archive:write
      x-hunter-body-transport: single_bounded_stream
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: branch_name
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - $ref: "#/components/parameters/RequestId"
        - name: Content-Length
          in: header
          required: true
          schema: { type: string, pattern: "^[1-9][0-9]{0,8}$", maxLength: 9 }
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        - name: X-Content-SHA256
          in: header
          required: true
          schema: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        - name: X-Upload-Expires-In-Ms
          in: header
          required: true
          schema: { type: string, pattern: "^[1-9][0-9]{0,8}$", maxLength: 9 }
        - name: X-Commit-SHA
          in: header
          schema: { type: string, minLength: 1, maxLength: 240 }
        - name: X-Client-Id
          in: header
          schema: { type: string, minLength: 1, maxLength: 240 }
        - name: X-Change-Key
          in: header
          schema: { type: string, minLength: 1, maxLength: 240 }
      requestBody:
        required: true
        content:
          application/zip:
            schema: { type: string, format: binary, maxLength: 536870912 }
      responses:
        "201":
          description: New bounded upload record
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteContentUploadHttpResult" } } }
        "200":
          description: Idempotent replay of the same complete request identity
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteContentUploadHttpResult" } } }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "410": { $ref: "#/components/responses/Error" }
        "413": { $ref: "#/components/responses/Error" }
        "415": { $ref: "#/components/responses/Error" }
        "499": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/branches/{branch_name}/remote-sync/content-upload/status:
    get:
      operationId: getRemoteContentUploadStatus
      summary: Resolve the scoped outcome of one remote content upload identity
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: archive:read
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: branch_name
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - $ref: "#/components/parameters/RequestId"
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        - name: X-Commit-SHA
          in: header
          schema: { type: string, minLength: 1, maxLength: 240 }
        - name: X-Client-Id
          in: header
          schema: { type: string, minLength: 1, maxLength: 240 }
        - name: X-Change-Key
          in: header
          schema: { type: string, minLength: 1, maxLength: 240 }
      responses:
        "200":
          description: Stored, expired, or unknown status for this complete scoped identity
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteContentUploadHttpStatus" } } }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/branches/{branch_name}/remote-sync/archive:prepare:
    post:
      operationId: prepareRemoteSyncArchive
      summary: Prepare a durable Remote Archive v2 operation for a stored bounded upload
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: archive:write
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED, REMOTE_ARCHIVE_INPUT_INVALID]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "409": [REMOTE_ARCHIVE_IDEMPOTENCY_CONFLICT, REMOTE_ARCHIVE_CAPABILITY_UNAVAILABLE]
        "422": [REMOTE_ARCHIVE_PAYLOAD_HASH_MISMATCH]
        "503": [REMOTE_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: branch_name
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/RemoteSyncArchivePrepareHttpRequest" } } }
      responses:
        "201":
          description: New durable prepare claim
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteSyncArchivePrepareHttpResponse" } } }
        "200":
          description: Idempotent replay without a new capability claim
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteSyncArchivePrepareHttpResponse" } } }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/branches/{branch_name}/remote-sync/archive:commit:
    post:
      operationId: commitRemoteSyncArchive
      summary: Commit a prepared Remote Archive v2 operation
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: archive:write
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED, REMOTE_ARCHIVE_INPUT_INVALID]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "404": [REMOTE_ARCHIVE_PREPARE_NOT_FOUND]
        "409": [REMOTE_ARCHIVE_LEASE_FENCED, REMOTE_ARCHIVE_LEASE_SCOPE_MISMATCH, REMOTE_ARCHIVE_CAPABILITY_UNAVAILABLE, REMOTE_ARCHIVE_COMMIT_AMBIGUOUS]
        "410": [REMOTE_ARCHIVE_PREPARE_EXPIRED]
        "503": [REMOTE_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: branch_name
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/RemoteSyncArchiveCommitHttpRequest" } } }
      responses:
        "200":
          description: Committed receipt, including a durable replay
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteSyncArchiveCommitHttpResponse" } } }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "410": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/branches/{branch_name}/remote-sync/archive/status:
    get:
      operationId: getRemoteSyncArchiveStatus
      summary: Resolve a scoped Remote Archive v2 operation
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: archive:read
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED, REMOTE_ARCHIVE_INPUT_INVALID]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "409": [REMOTE_ARCHIVE_LEASE_SCOPE_MISMATCH]
        "503": [REMOTE_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: branch_name
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: operation_id
          in: query
          required: true
          schema: { type: string, pattern: "^remote_archive_operation:.{1,215}$" }
        - name: actor_id
          in: query
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: commit_sha
          in: query
          required: false
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: client_id
          in: query
          required: false
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: change_key
          in: query
          required: false
          schema: { type: string, minLength: 1, maxLength: 160 }
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Prepared, committed, failed, or unknown durable status
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteSyncArchiveStatusHttpResponse" } } }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/branches/{branch_name}/remote-sync/archive/{operation_id}/receipt:
    get:
      operationId: getRemoteSyncArchiveReceipt
      summary: Resolve the durable receipt for a Remote Archive v2 operation
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: archive:read
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED, REMOTE_ARCHIVE_INPUT_INVALID]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "409": [REMOTE_ARCHIVE_LEASE_SCOPE_MISMATCH]
        "503": [REMOTE_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: branch_name
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: operation_id
          in: path
          required: true
          schema: { type: string, pattern: "^remote_archive_operation:.{1,215}$" }
        - name: actor_id
          in: query
          required: true
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: commit_sha
          in: query
          required: false
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: client_id
          in: query
          required: false
          schema: { type: string, minLength: 1, maxLength: 160 }
        - name: change_key
          in: query
          required: false
          schema: { type: string, minLength: 1, maxLength: 160 }
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Durable receipt or null for a non-committed operation
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content: { application/json: { schema: { $ref: "#/components/schemas/RemoteSyncArchiveReceiptHttpResponse" } } }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/semantic/overview:
    get:
      operationId: getProjectSemanticOverview
      responses:
        "200": { description: Semantic index overview counts for a project }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/semantic/knowledge:
    get:
      operationId: listProjectSemanticKnowledge
      responses:
        "200":
          {
            description: Knowledge documents derived from the latest project artifact,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/semantic/rules:
    get:
      operationId: listProjectSemanticRules
      responses:
        "200":
          {
            description: Rule documents derived from the latest project artifact,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/semantic/architecture:
    get:
      operationId: listProjectSemanticArchitecture
      responses:
        "200":
          {
            description: Codebase Map documents derived from the latest managed project snapshot,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/semantic/changes:
    get:
      operationId: listProjectSemanticChanges
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
        - name: cursor
          in: query
          schema: { type: string }
      responses:
        "200":
          {
            description: Paginated archive change documents derived from the latest project artifact,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/semantic/graph:
    get:
      operationId: getProjectSemanticGraph
      responses:
        "200": { description: Semantic graph nodes and edges for a project }
        default: { $ref: "#/components/responses/Error" }
  /mcp:
    post:
      operationId: semanticMcpStreamableHttp
      summary: Semantic MCP server (Streamable HTTP transport). GET/DELETE return 405; use POST only.
      description: >-
        JSON-RPC 2.0 endpoint for the read-only Semantic MCP server (tools: search_knowledge,
        get_project_overview, get_knowledge_entry, list_recent_changes). Stateless per request;
        no MCP session id is issued or required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: JSON-RPC 2.0 request envelope (initialize, tools/list, tools/call, ...)
      responses:
        "200":
          description: JSON-RPC 2.0 response (or an SSE stream) for the invoked Semantic MCP method
        "401":
          description: Auth required (AUTH_REQUIRED)
        default:
          $ref: "#/components/responses/Error"
  /api/v1/semantic/search:
    get:
      operationId: searchSemanticDocuments
      parameters:
        - name: q
          in: query
          required: true
          schema: { type: string }
        - name: project_id
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          {
            description: Cross-project or project-scoped FTS search over semantic documents,
          }
        default: { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/semantic/search:
    get:
      operationId: searchProjectSemanticDocuments
      description: Search one project's semantic documents. Project API keys require knowledge:read.
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: q
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          { description: Project-scoped FTS search over semantic documents }
        "403":
          {
            description: Project key lacks knowledge:read or belongs to another project,
          }
        default: { $ref: "#/components/responses/Error" }

  /api/v1/projects/{project_id}/knowledge/query:
    post:
      operationId: queryProjectKnowledge
      summary: Execute one bounded, receipt-backed remote knowledge query
      description: The actor, project allowlist and knowledge:read scope come from authenticated server authority. The response contains bounded summaries only; document bodies are not returned.
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: "knowledge:read"
      x-hunter-idempotency-header: Idempotency-Key
      x-hunter-request-id-header: X-Request-Id
      x-hunter-error-codes:
        "400": [KNOWLEDGE_QUERY_INVALID]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "409": [KNOWLEDGE_QUERY_IDEMPOTENCY_CONFLICT, KNOWLEDGE_QUERY_SNAPSHOT_STALE]
        "422": [KNOWLEDGE_QUERY_RECEIPT_INVALID]
        "499": [KNOWLEDGE_QUERY_ABORTED]
        "503": [REMOTE_UNAVAILABLE, KNOWLEDGE_QUERY_TIMEOUT]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/KnowledgeQueryIdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KnowledgeQueryHttpRequest" }
      responses:
        "201":
          description: Query executed and its durable receipt was created
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeQueryHttpResponse" }
        "200":
          description: Idempotent replay of an existing query receipt
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeQueryHttpResponse" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }
        "499": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }

  /api/v1/projects/{project_id}/information/{view}:
    get:
      operationId: listPlatformInformation
      summary: List one bounded project information view
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-normalizer-id: normalizePlatformInformationListHttpQuery
      x-hunter-project-key-scope-by-view:
        {
          branch_monitor: "platform:read",
          branch_files: "files:read",
          project_materials: "files:read",
          project_knowledge: "knowledge:read",
          change_records: "files:read",
          version_records: "files:read",
        }
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED, PLATFORM_INFORMATION_CURSOR_INVALID]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "503": [PLATFORM_INFORMATION_SOURCE_INVALID, PLATFORM_INFORMATION_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/PlatformInformationView"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Canonical bounded information page
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PlatformInformationPage" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/information/{view}:export-all:
    get:
      operationId: exportAllPlatformInformation
      summary: Walk every authorized page to a terminal cursor and return its verified proof
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: "platform:read"
      x-hunter-normalizer-id: normalizePlatformInformationListHttpQuery
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED, PLATFORM_INFORMATION_CURSOR_INVALID]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "503": [PLATFORM_INFORMATION_SOURCE_INVALID, PLATFORM_INFORMATION_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/PlatformInformationView"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Complete verified export cursor proof
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PlatformInformationExportResult" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/information/{view}:export:
    post:
      operationId: createPlatformInformationExport
      summary: Create or replay a durable platform information export
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope-by-view:
        {
          branch_monitor: "platform:read",
          branch_files: "files:read",
          project_materials: "files:read",
          project_knowledge: "knowledge:read",
          change_records: "files:read",
          version_records: "files:read",
        }
      x-hunter-idempotency-header: Idempotency-Key
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "409": [PLATFORM_INFORMATION_EXPORT_IDEMPOTENCY_CONFLICT]
        "410": [PLATFORM_INFORMATION_EXPORT_EXPIRED]
        "422": [PLATFORM_INFORMATION_EXPORT_IDEMPOTENCY_REQUIRED]
        "503": [PLATFORM_INFORMATION_EXPORT_SOURCE_INVALID, PLATFORM_INFORMATION_EXPORT_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/PlatformInformationView"
        - $ref: "#/components/parameters/RequestId"
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PlatformInformationExportCreateHttpRequest" }
      responses:
        "201":
          description: Newly created durable export receipt
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PlatformInformationExportArtifactReceipt" }
        "200":
          description: Idempotent replay of an existing durable export receipt
          headers: { X-Request-Id: { $ref: "#/components/headers/RequestId" } }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PlatformInformationExportArtifactReceipt" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "410": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/information/{view}/exports/{export_id}:
    get:
      operationId: downloadPlatformInformationExport
      summary: Stream a ready platform information export artifact
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope-by-view:
        {
          branch_monitor: "platform:read",
          branch_files: "files:read",
          project_materials: "files:read",
          project_knowledge: "knowledge:read",
          change_records: "files:read",
          version_records: "files:read",
        }
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "404": [PLATFORM_INFORMATION_EXPORT_NOT_FOUND]
        "410": [PLATFORM_INFORMATION_EXPORT_EXPIRED]
        "503": [PLATFORM_INFORMATION_EXPORT_DOWNLOAD_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/PlatformInformationView"
        - name: export_id
          in: path
          required: true
          schema: { type: string, pattern: "^export_[A-Za-z0-9_-]{1,156}$" }
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Canonical JSONL export stream
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
            Content-Disposition: { schema: { type: string } }
            X-Content-SHA256:
              schema: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
          content:
            application/x-ndjson:
              schema: { type: string, format: binary }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "410": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/information/{view}/{detail_id}:
    get:
      operationId: getPlatformInformationDetail
      summary: Read one authorized project information detail by stable identity
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope-by-view:
        {
          branch_monitor: "platform:read",
          branch_files: "files:read",
          project_materials: "files:read",
          project_knowledge: "knowledge:read",
          change_records: "files:read",
          version_records: "files:read",
        }
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "404": [PLATFORM_INFORMATION_DETAIL_NOT_FOUND]
        "503": [PLATFORM_INFORMATION_SOURCE_INVALID, PLATFORM_INFORMATION_UNAVAILABLE]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/PlatformInformationView"
        - $ref: "#/components/parameters/PlatformInformationDetailId"
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Canonical information detail
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PlatformInformationDetailResponse" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/information/branch-files:preview-restore:
    post:
      operationId: previewBranchFilesRestore
      summary: Create a read-only branch file restore preview
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: "files:read"
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "422": [BRANCH_FILES_RESTORE_PREVIEW_INVALID]
        "503": [PLATFORM_INFORMATION_SOURCE_INVALID, PLATFORM_INFORMATION_UNAVAILABLE]
      parameters:
        [
          { $ref: "#/components/parameters/ProjectId" },
          { $ref: "#/components/parameters/RequestId" },
        ]
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                {
                  schema:
                    { $ref: "#/components/schemas/RestoreBranchFilesIntent" },
                },
            },
        }
      responses:
        "200":
          description: Canonical read-only restore preview receipt
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RestoreBranchFilesPreviewReceipt" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/information/branch-files:confirm-restore:
    post:
      operationId: confirmBranchFilesRestore
      summary: Validate preview-bound conflict decisions and return a request-only Pull intent
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: "files:read"
      x-hunter-validator-id: validatePlatformInformationConfirmRestoreHttpRequest
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "409": [BRANCH_FILES_PULL_CONFIRMATION_MISMATCH]
        "422": [BRANCH_FILES_PULL_CONFIRMATION_INVALID]
      parameters:
        [
          { $ref: "#/components/parameters/ProjectId" },
          { $ref: "#/components/parameters/RequestId" },
        ]
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                {
                  schema:
                    {
                      $ref: "#/components/schemas/PlatformInformationConfirmRestoreHttpRequest",
                    },
                },
            },
        }
      responses:
        "200":
          description: Canonical request-only confirmed Pull intent
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RestoreBranchFilesConfirmedIntent" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }
  /api/v1/projects/{project_id}/information/knowledge:retry-extraction:
    post:
      operationId: retryProjectKnowledgeExtraction
      summary: Create a request-only knowledge extraction retry intent
      x-hunter-auth-source: authenticated_principal
      x-hunter-project-allowlist-source: server_authority
      x-hunter-project-key-scope: "knowledge:write"
      x-hunter-error-codes:
        "400": [VALIDATION_FAILED]
        "401": [AUTH_REQUIRED, TOKEN_INVALID, SESSION_INVALID]
        "403": [PROJECT_INFORMATION_FORBIDDEN, PROJECT_KEY_SCOPE, PROJECT_KEY_MISMATCH]
        "404": [KNOWLEDGE_EXTRACTION_JOB_NOT_FOUND]
        "409": [KNOWLEDGE_EXTRACTION_GENERATION_CONFLICT]
        "422": [KNOWLEDGE_EXTRACTION_RETRY_REQUEST_INVALID]
        "503": [KNOWLEDGE_EXTRACTION_RETRY_INTENT_INVALID, KNOWLEDGE_EXTRACTION_RETRY_AUTHORITY_INVALID, PLATFORM_INFORMATION_UNAVAILABLE]
      parameters:
        [
          { $ref: "#/components/parameters/ProjectId" },
          { $ref: "#/components/parameters/RequestId" },
        ]
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                {
                  schema:
                    {
                      $ref: "#/components/schemas/PlatformInformationRetryExtractionHttpRequest",
                    },
                },
            },
        }
      responses:
        "200":
          description: Canonical request-only retry intent
          headers:
            X-Request-Id: { $ref: "#/components/headers/RequestId" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeExtractionRetryIntent" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }
        "503": { $ref: "#/components/responses/Error" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  parameters:
    RequestId:
      name: X-Request-Id
      in: header
      schema:
        type: string
        format: uuid
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        format: uuid
    KnowledgeQueryIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 1
        maxLength: 240
        pattern: "^[!-~]+$"
    ProjectId:
      name: project_id
      in: path
      required: true
      schema:
        type: string
        pattern: "^prj_[A-Za-z0-9_-]{1,124}$"
    PlatformInformationView:
      name: view
      in: path
      required: true
      schema: { $ref: "#/components/schemas/PlatformInformationView" }
    PlatformInformationDetailId:
      name: detail_id
      in: path
      required: true
      schema: { type: string, minLength: 1, maxLength: 160 }
    ChangeKey:
      name: change_key
      in: path
      required: true
      schema:
        type: string
        pattern: "^[A-Za-z0-9][A-Za-z0-9._-]{0,159}$"
    SessionId:
      name: session_id
      in: path
      required: true
      schema:
        type: string
    ProposalId:
      name: proposal_id
      in: path
      required: true
      schema:
        type: string
    ArtifactId:
      name: artifact_id
      in: path
      required: true
      schema:
        type: string
    ContentSha256:
      name: content_sha256
      in: path
      required: true
      schema:
        type: string
    Cursor:
      name: cursor
      in: query
      schema:
        type:
          - string
          - "null"
    Limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    SkillSlug:
      name: slug
      in: path
      required: true
      schema:
        type: string
    Agent:
      name: agent
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/RegistryAgent"
  schemas:
    SchemaVersionRequest:
      type: object
      additionalProperties: false
      required: [schema_version]
      properties:
        schema_version: { type: integer, const: 1 }
    NpmPublishingCredentialReplaceRequest:
      type: object
      additionalProperties: false
      required: [schema_version, token, expires_at]
      properties:
        schema_version: { type: integer, const: 1 }
        token:
          type: string
          minLength: 1
          maxLength: 2048
          writeOnly: true
          format: password
          description: npm Granular Access Token; never returned or logged
        expires_at:
          type: [string, "null"]
          format: date-time
          description: Operator-recorded token expiration used to block expired publishing credentials
    NpmPublishingCredentialStatus:
      type: object
      additionalProperties: false
      required:
        [
          scope,
          source,
          state,
          username,
          expires_at,
          last_verified_at,
          can_manage,
        ]
      properties:
        scope: { type: [string, "null"] }
        source: { type: string, enum: [none, deployment, managed] }
        state:
          {
            type: string,
            enum: [not_configured, configured, ready, expired, invalid, locked],
          }
        username: { type: [string, "null"] }
        expires_at: { type: [string, "null"], format: date-time }
        last_verified_at: { type: [string, "null"], format: date-time }
        can_manage:
          type: boolean
          description: Whether the server has a valid encryption key for page-managed credentials
        request_id: { type: string, format: uuid }
    CodexModelOption:
      type: object
      additionalProperties: false
      required: [id, display_name, is_default, reasoning_efforts]
      properties:
        id: { type: string }
        display_name: { type: string }
        is_default: { type: boolean }
        reasoning_efforts:
          type: array
          items: { type: string }
    CodexConnectionState:
      type: object
      additionalProperties: true
      required:
        [
          status,
          auth_mode,
          email,
          plan_type,
          enabled,
          selected_model,
          models,
          error,
        ]
      properties:
        status: { type: string, enum: [connected, disconnected, unavailable] }
        auth_mode: { type: [string, "null"], enum: [chatgpt, null] }
        email: { type: [string, "null"] }
        plan_type: { type: [string, "null"] }
        enabled:
          type: boolean
          description: Whether Codex is the single platform-wide active model source
        selected_model: { type: [string, "null"] }
        models:
          type: array
          items: { $ref: "#/components/schemas/CodexModelOption" }
        error: { type: [string, "null"] }
        request_id: { type: string, format: uuid }
    CodexLoginStart:
      type: object
      additionalProperties: true
      required: [login_id, verification_url, user_code]
      properties:
        login_id: { type: string }
        verification_url: { type: string, format: uri }
        user_code: { type: string }
        request_id: { type: string, format: uuid }
    DashboardOverview:
      type: object
      additionalProperties: false
      required:
        - generated_at
        - window
        - metrics
        - trend
        - distributions
        - ai_usage
        - active_runs
        - health
        - services
        - activity
      properties:
        generated_at:
          type: string
          format: date-time
        window:
          type: object
          required: [days, starts_at, ends_at]
        metrics:
          type: object
          description: Counts sourced from the authenticated actor's project repository and canonical Skill Registry.
        trend:
          type: array
          description: Daily proposal submissions and recorded review outcomes for the requested window.
          items:
            type: object
        distributions:
          type: object
          description: Skill, Workflow, and project knowledge distribution counts.
        ai_usage:
          type: array
          description: Daily AI request, token, and estimated cost totals for the requested window.
          items:
            type: object
        active_runs:
          type: array
          description: Most recently active project runs visible to the authenticated actor.
          items:
            type: object
        health:
          type: array
          description: Explainable governance checks; unavailable means insufficient evidence rather than success.
          items:
            type: object
        services:
          type: array
          description: Data-source read signals, not synthetic infrastructure telemetry.
          items:
            type: object
        activity:
          type: array
          description: Sanitized audit event metadata; audit details are never returned.
          items:
            type: object
    ProjectResolveRequest:
      type: object
      additionalProperties: false
      required:
        - schema_version
        - local_project_key
        - display_name
        - client_id
      properties:
        schema_version:
          const: 1
        local_project_key:
          type: string
          format: uuid
        display_name:
          type: string
        requested_project_id:
          type:
            - string
            - "null"
        client_id:
          type: string
    ApiError:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          type: object
          additionalProperties: false
          required:
            - code
            - message
            - request_id
            - details
          properties:
            code:
              type: string
            message:
              type: string
            request_id:
              type: string
              format: uuid
            details:
              type: object
    RegistryAgent:
      type: string
      enum: ["claude-code", "codex", "cursor", "codebuddy", "generic", "mcp"]
    SkillTargetAgent:
      type: string
      enum: ["claude-code", "codex", "cursor", "codebuddy"]
    RegistrySlug:
      type: string
      pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    AgentToolSource:
      type: object
      additionalProperties: false
      required: [type, ref]
      properties:
        type: { type: string, enum: [github, npm, website] }
        ref:
          type: string
          minLength: 1
          maxLength: 1000
          description: Exact curator-provided source; GitHub tree branch and subpath are preserved
    CreateAgentToolRequest:
      type: object
      additionalProperties: false
      required:
        [schema_version, slug, displayName, description, category, source]
      properties:
        schema_version: { type: integer, const: 1 }
        slug: { $ref: "#/components/schemas/RegistrySlug" }
        displayName: { type: string, minLength: 1, maxLength: 120 }
        description: { type: string, minLength: 1, maxLength: 1200 }
        category:
          {
            type: string,
            enum: [harness, runtime, orchestrator, ade, cli, framework],
          }
        status:
          {
            type: string,
            enum: [active, experimental, archived],
            default: active,
          }
        source: { $ref: "#/components/schemas/AgentToolSource" }
        homepage: { type: [string, "null"], format: uri }
        packageName: { type: [string, "null"], maxLength: 220 }
        installCommand: { type: [string, "null"], maxLength: 500 }
        tags:
          type: array
          items: { $ref: "#/components/schemas/RegistrySlug" }
          default: []
        relatedWorkflowFamilies:
          type: array
          items: { $ref: "#/components/schemas/RegistrySlug" }
          default: []
    AgentTool:
      type: object
      additionalProperties: false
      required:
        [
          tool_id,
          slug,
          displayName,
          description,
          category,
          status,
          source,
          homepage,
          packageName,
          installCommand,
          tags,
          relatedWorkflowFamilies,
          revision,
          created_at,
          updated_at,
        ]
      properties:
        tool_id: { type: string, pattern: "^atl_" }
        slug: { $ref: "#/components/schemas/RegistrySlug" }
        displayName: { type: string }
        description: { type: string }
        category:
          {
            type: string,
            enum: [harness, runtime, orchestrator, ade, cli, framework],
          }
        status: { type: string, enum: [active, experimental, archived] }
        source: { $ref: "#/components/schemas/AgentToolSource" }
        homepage: { type: [string, "null"], format: uri }
        packageName: { type: [string, "null"] }
        installCommand: { type: [string, "null"] }
        tags:
          { type: array, items: { $ref: "#/components/schemas/RegistrySlug" } }
        relatedWorkflowFamilies:
          { type: array, items: { $ref: "#/components/schemas/RegistrySlug" } }
        revision: { type: integer, minimum: 1 }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        request_id: { type: string, format: uuid }
    InspectAgentToolGithubRequest:
      type: object
      additionalProperties: false
      required: [schema_version, github_url]
      properties:
        schema_version: { type: integer, const: 1 }
        github_url: { type: string, minLength: 1, maxLength: 1000 }
    AgentToolGithubInspection:
      type: object
      additionalProperties: false
      required: [source, suggested]
      properties:
        source: { $ref: "#/components/schemas/AgentToolSource" }
        suggested: { $ref: "#/components/schemas/AgentToolSuggestion" }
        request_id: { type: string, format: uuid }
    GenerateAgentToolPrefillRequest:
      type: object
      additionalProperties: false
      required: [schema_version, inspection]
      properties:
        schema_version: { type: integer, const: 1 }
        inspection:
          { $ref: "#/components/schemas/AgentToolGithubInspectionInput" }
    AgentToolGithubInspectionInput:
      type: object
      additionalProperties: false
      required: [source, suggested]
      properties:
        source: { $ref: "#/components/schemas/AgentToolSource" }
        suggested: { $ref: "#/components/schemas/AgentToolSuggestion" }
    AgentToolSuggestion:
      type: object
      additionalProperties: false
      required:
        [
          slug,
          displayName,
          description,
          category,
          status,
          source,
          tags,
          relatedWorkflowFamilies,
        ]
      properties:
        slug: { $ref: "#/components/schemas/RegistrySlug" }
        displayName: { type: string, minLength: 1, maxLength: 120 }
        description: { type: string, minLength: 1, maxLength: 1200 }
        category:
          {
            type: string,
            enum: [harness, runtime, orchestrator, ade, cli, framework],
          }
        status: { type: string, enum: [active, experimental, archived] }
        source: { $ref: "#/components/schemas/AgentToolSource" }
        homepage: { type: [string, "null"], format: uri }
        packageName: { type: [string, "null"], maxLength: 220 }
        installCommand: { type: [string, "null"], maxLength: 500 }
        tags:
          { type: array, items: { $ref: "#/components/schemas/RegistrySlug" } }
        relatedWorkflowFamilies:
          { type: array, items: { $ref: "#/components/schemas/RegistrySlug" } }
    WorkflowFamilySource:
      type: object
      additionalProperties: false
      required: [type, ref]
      properties:
        type: { type: string, enum: [npm, github] }
        ref:
          type: string
          minLength: 1
          maxLength: 1000
          description: npm package name or exact GitHub repository/tree URL
    InspectWorkflowFamilySourceRequest:
      type: object
      additionalProperties: false
      required: [schema_version, source]
      properties:
        schema_version: { type: integer, const: 1 }
        source: { $ref: "#/components/schemas/WorkflowFamilySource" }
    WorkflowFamilySourceInspection:
      type: object
      additionalProperties: false
      required:
        [
          source,
          remote_version,
          source_digest,
          manifest_detected,
          ready,
          suggested,
          profiles,
          warnings,
        ]
      properties:
        source: { $ref: "#/components/schemas/WorkflowFamilySource" }
        remote_version:
          { type: [string, "null"], pattern: "^\\d+\\.\\d+\\.\\d+$" }
        source_digest: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        manifest_detected: { type: boolean }
        ready: { type: boolean }
        suggested:
          type: object
          additionalProperties: false
          required: [slug, displayName, description, tags]
          properties:
            slug: { $ref: "#/components/schemas/RegistrySlug" }
            displayName: { type: string }
            description: { type: string }
            tags:
              {
                type: array,
                items: { $ref: "#/components/schemas/RegistrySlug" },
              }
        profiles:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [profile, file_count]
            properties:
              profile: { $ref: "#/components/schemas/RegistrySlug" }
              file_count: { type: integer, minimum: 1 }
        warnings: { type: array, items: { type: string } }
        request_id: { type: string, format: uuid }
    WorkflowFamilySyncResult:
      type: object
      additionalProperties: false
      required: [updated, request_id]
      properties:
        updated: { type: boolean }
        version: { type: string, pattern: "^\\d+\\.\\d+\\.\\d+$" }
        request_id: { type: string, format: uuid }
    ImportWorkflowFamilySourceRequest:
      type: object
      additionalProperties: false
      required:
        [schema_version, source, source_digest, slug, displayName, description]
      properties:
        schema_version: { type: integer, const: 1 }
        source: { $ref: "#/components/schemas/WorkflowFamilySource" }
        source_digest: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        slug: { $ref: "#/components/schemas/RegistrySlug" }
        displayName: { type: string, minLength: 1, maxLength: 120 }
        description: { type: string, minLength: 1, maxLength: 1000 }
        tags:
          {
            type: array,
            items: { $ref: "#/components/schemas/RegistrySlug" },
            default: [],
          }
    WorkflowFamilySourceImportResult:
      type: object
      additionalProperties: false
      required: [family, draft, inspection, request_id]
      properties:
        family:
          type: object
          additionalProperties: false
          description: Created WorkflowFamily registry record
          required:
            [
              family_id,
              slug,
              displayName,
              description,
              tags,
              latest_version,
              required_profiles,
              revision,
              npmReleases,
              source,
              created_at,
              updated_at,
            ]
          properties:
            family_id: { type: string, pattern: "^wff_" }
            slug: { $ref: "#/components/schemas/RegistrySlug" }
            displayName: { type: string, minLength: 1, maxLength: 120 }
            description: { type: string, minLength: 1, maxLength: 1000 }
            tags:
              {
                type: array,
                items: { $ref: "#/components/schemas/RegistrySlug" },
              }
            latest_version:
              { type: [string, "null"], pattern: "^\\d+\\.\\d+\\.\\d+$" }
            required_profiles:
              {
                type: array,
                minItems: 1,
                items: { $ref: "#/components/schemas/RegistrySlug" },
              }
            revision: { type: integer, minimum: 1 }
            npmReleases:
              type: array
              items:
                type: object
                additionalProperties: false
                required: [version, packageName, status, publishedAt, error]
                properties:
                  version: { type: string, pattern: "^\\d+\\.\\d+\\.\\d+$" }
                  packageName: { type: string, minLength: 1 }
                  status: { type: string, enum: [published, failed, conflict] }
                  publishedAt: { type: string, format: date-time }
                  error: { type: [string, "null"] }
            source: { $ref: "#/components/schemas/WorkflowFamilySource" }
            created_at: { type: string, format: date-time }
            updated_at: { type: string, format: date-time }
        draft:
          type: object
          additionalProperties: false
          description: Lightweight staged draft metadata; source contents remain in artifact storage
          required:
            [
              family_slug,
              profiles,
              required_profiles,
              draftVersion,
              checks,
              releaseNote,
              revision,
              created_at,
              updated_at,
            ]
          properties:
            family_slug: { $ref: "#/components/schemas/RegistrySlug" }
            profiles:
              type: array
              minItems: 1
              items:
                type: object
                additionalProperties: false
                required: [profile, file_count]
                properties:
                  profile: { $ref: "#/components/schemas/RegistrySlug" }
                  file_count: { type: integer, minimum: 0 }
            required_profiles:
              {
                type: array,
                minItems: 1,
                items: { $ref: "#/components/schemas/RegistrySlug" },
              }
            draftVersion:
              { type: [string, "null"], pattern: "^\\d+\\.\\d+\\.\\d+$" }
            checks:
              oneOf:
                - type: "null"
                - type: object
                  additionalProperties: false
                  required: [items, summary, checkedAt]
                  properties:
                    items:
                      type: array
                      items:
                        type: object
                        additionalProperties: false
                        required:
                          [id, label, status, message, filePath, fixable]
                        properties:
                          id: { type: string }
                          label: { type: string }
                          status: { type: string, enum: [green, yellow, red] }
                          message: { type: string }
                          filePath: { type: [string, "null"] }
                          fixable: { type: boolean }
                    summary:
                      type: object
                      additionalProperties: false
                      required: [green, yellow, red]
                      properties:
                        green: { type: integer, minimum: 0 }
                        yellow: { type: integer, minimum: 0 }
                        red: { type: integer, minimum: 0 }
                    checkedAt: { type: string }
            releaseNote: { type: [string, "null"] }
            revision: { type: integer, minimum: 1 }
            created_at: { type: string }
            updated_at: { type: string }
            request_id: { type: string, format: uuid }
        inspection:
          { $ref: "#/components/schemas/WorkflowFamilySourceInspection" }
        request_id: { type: string, format: uuid }
    WorkflowFamilyVersionSummary:
      type: object
      additionalProperties: false
      required:
        [family_slug, version, profiles, artifacts, changeNote, created_at]
      properties:
        family_slug: { $ref: "#/components/schemas/RegistrySlug" }
        version: { type: string, pattern: "^\\d+\\.\\d+\\.\\d+$" }
        profiles:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [profile, bundle_manifest, artifact_id, file_count]
            properties:
              profile: { $ref: "#/components/schemas/RegistrySlug" }
              artifact_id: { type: string, pattern: "^wfb_" }
              file_count: { type: integer, minimum: 0 }
              bundle_manifest:
                type: object
                additionalProperties: false
                required: [schema_version, profile, files]
                properties:
                  schema_version: { type: integer, const: 1 }
                  profile: { $ref: "#/components/schemas/RegistrySlug" }
                  files:
                    type: array
                    minItems: 1
                    items:
                      type: object
                      additionalProperties: false
                      required: [path, sha256]
                      properties:
                        path: { type: string, minLength: 1 }
                        sha256:
                          { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        artifacts:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              [
                artifact_id,
                family_slug,
                profile,
                version,
                content_sha256,
                size_bytes,
                bundle_manifest,
                created_at,
              ]
            properties:
              artifact_id: { type: string, pattern: "^wfb_" }
              family_slug: { $ref: "#/components/schemas/RegistrySlug" }
              profile: { $ref: "#/components/schemas/RegistrySlug" }
              version: { type: string, pattern: "^\\d+\\.\\d+\\.\\d+$" }
              content_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
              size_bytes: { type: integer, minimum: 0 }
              bundle_manifest:
                {
                  $ref: "#/components/schemas/WorkflowFamilyVersionSummary/properties/profiles/items/properties/bundle_manifest",
                }
              created_at: { type: string, format: date-time }
        changeNote: { type: [string, "null"] }
        created_at: { type: string, format: date-time }
        request_id: { type: string, format: uuid }
    SensitiveReviewSubmission:
      type: object
      additionalProperties: false
      required: [scanner_version, finding_fingerprints, reason]
      properties:
        scanner_version: { type: string, minLength: 1 }
        finding_fingerprints:
          type: array
          minItems: 1
          items: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        reason: { type: string, minLength: 3, maxLength: 500 }
    PublishUnifiedSkillRequest:
      type: object
      additionalProperties: false
      required: [version, sourceAgent, draftRevision]
      properties:
        version: { type: string, pattern: "^\\d+\\.\\d+\\.\\d+$" }
        sourceAgent: { $ref: "#/components/schemas/SkillTargetAgent" }
        draftRevision: { type: integer, minimum: 1 }
        releaseNote: { type: string, minLength: 1, maxLength: 2000 }
    PublishSkillResponse:
      type: object
      additionalProperties: false
      required: [release, npmRelease]
      properties:
        release:
          type: object
          additionalProperties: false
          required: [slug, version]
          properties:
            slug: { type: string }
            version: { type: string }
        npmRelease:
          type: object
          additionalProperties: false
          required: [status, packageName, version, tarballHash]
          properties:
            status: { type: string, enum: [published, idempotent] }
            packageName: { type: string }
            version: { type: string }
            tarballHash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    SetDefaultAgentRequest:
      type: object
      additionalProperties: false
      required:
        - defaultAgent
        - revision
      properties:
        defaultAgent:
          $ref: "#/components/schemas/RegistryAgent"
        revision:
          type: integer
          minimum: 1
    FinalizeProposalRequest:
      type: object
      additionalProperties: false
      required:
        - schema_version
        - manifest_sha256
        - base_artifact_id
      properties:
        schema_version:
          type: integer
          const: 1
        manifest_sha256:
          type: string
          pattern: "^sha256:[a-f0-9]{64}$"
        base_artifact_id:
          type:
            - string
            - "null"
          pattern: "^art_"
        sensitive_scan_skip:
          type: boolean
          const: true
          description: Explicit opt-in to finalize despite blocked sensitive scan findings (project push only)
        sensitive_scan_skip_reason:
          type: string
          maxLength: 500
          description: Optional audit reason; requires sensitive_scan_skip=true
    ArchivePackageReceipt:
      type: object
      additionalProperties: false
      required:
        - schema_version
        - archive_id
        - project_id
        - change_key
        - package_sha256
        - manifest_sha256
        - artifact_id
        - archive_status
        - knowledge_status
        - stored_files
        - uploaded_at
        - request_id
      properties:
        schema_version: { type: integer, const: 1 }
        archive_id: { type: string, pattern: "^arc_" }
        project_id: { type: string, pattern: "^prj_" }
        change_key: { type: string }
        package_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        manifest_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        artifact_id:
          type: [string, "null"]
        archive_status: { type: string, const: durable }
        knowledge_status: { type: string, enum: [indexing, ready, failed] }
        stored_files: { type: integer, minimum: 0 }
        uploaded_at: { type: string, format: date-time }
        request_id: { type: string, format: uuid }
    ContentKind:
      type: string
      enum:
        - config
        - rule
        - architecture
        - instruction
        - branch_file
        - change_document
        - archive_package
        - knowledge_entry
        - knowledge_candidate
        - project_content_candidate
    SyncScope:
      type: string
      enum: [config, rules, architecture, instructions, branch_files, archive]
    SyncDirection:
      type: string
      enum: [push, pull]
    SyncAction:
      type: string
      enum: [add, modify, delete, restore, rename, no_change]
    ConflictResolution:
      type: string
      enum: [keep_local, accept_remote, skip, cancel]
    PullPolicy:
      type: string
      enum: [regular, explicit_source_only, not_pullable]
    ContentScanPolicy:
      type: string
      enum: [required, skip_content_scan]
    ProjectContentCandidateType:
      type: string
      enum: [rule, architecture-decision, glossary]
    ProjectContentCandidateStatus:
      type: string
      enum: [pending, proposed, accepted, rejected, superseded]
    KnowledgeCandidateStatus:
      type: string
      enum: [pending, accepted, rejected, superseded]
    CandidateProvenanceSourceKind:
      type: string
      enum: [archive, plan, review, manual, migration]
    ArchiveStatusValue:
      type: string
      enum: [absent, uploading, stored, failed]
    ChangeIndexStatusValue:
      type: string
      enum: [not_scheduled, queued, indexing, ready, failed]
    KnowledgeExtractionStatusValue:
      type: string
      enum: [not_scheduled, queued, extracting, ready, failed]
    ManagedSnapshotStatusValue:
      type: string
      enum: [absent, publishing, ready, conflict, failed]
    ContentSyncReasonCode:
      type: string
      minLength: 1
      maxLength: 128
      pattern: "^[A-Z][A-Z0-9_]*$"
    ArchiveStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { $ref: "#/components/schemas/ArchiveStatusValue" }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean }
        reason_code: { $ref: "#/components/schemas/ContentSyncReasonCode" }
    ChangeIndexStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { $ref: "#/components/schemas/ChangeIndexStatusValue" }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean }
        reason_code: { $ref: "#/components/schemas/ContentSyncReasonCode" }
    KnowledgeExtractionStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { $ref: "#/components/schemas/KnowledgeExtractionStatusValue" }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean }
        reason_code: { $ref: "#/components/schemas/ContentSyncReasonCode" }
    ManagedSnapshotStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { $ref: "#/components/schemas/ManagedSnapshotStatusValue" }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean }
        reason_code: { $ref: "#/components/schemas/ContentSyncReasonCode" }
    ContentSyncStatuses:
      type: object
      additionalProperties: false
      description: Current split-status projection; legacy knowledge_status is not mapped into these independent states.
      required:
        - schema_version
        - archive_status
        - change_index_status
        - knowledge_extraction_status
        - managed_snapshot_status
      properties:
        schema_version: { type: integer, const: 1 }
        archive_status: { $ref: "#/components/schemas/ArchiveStatus" }
        change_index_status: { $ref: "#/components/schemas/ChangeIndexStatus" }
        knowledge_extraction_status:
          { $ref: "#/components/schemas/KnowledgeExtractionStatus" }
        managed_snapshot_status:
          { $ref: "#/components/schemas/ManagedSnapshotStatus" }
    ArchiveIngestReceipt:
      oneOf:
        - $ref: "#/components/schemas/ArchiveIngestQueuedReceipt"
        - $ref: "#/components/schemas/ArchiveIngestPlanningFailedReceipt"
      description: Canonical v1 durable archive acknowledgement. A stored archive must not be re-uploaded even when atomic downstream task planning failed.
    ArchiveIngestStoredStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { type: string, const: stored }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean, const: false }
    ArchiveIngestQueuedStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { type: string, const: queued }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean, const: true }
    ArchiveIngestFailedStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable, reason_code]
      properties:
        status: { type: string, const: failed }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean, const: true }
        reason_code: { $ref: "#/components/schemas/ContentSyncReasonCode" }
    ArchiveIngestIndexingStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { type: string, const: indexing }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean, const: true }
    ArchiveIngestExtractingStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { type: string, const: extracting }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean, const: true }
    ArchiveIngestReadyStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable]
      properties:
        status: { type: string, const: ready }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean, const: false }
    ArchiveIngestJobFailedStatus:
      type: object
      additionalProperties: false
      required: [status, updated_at, retryable, reason_code]
      properties:
        status: { type: string, const: failed }
        updated_at: { type: string, format: date-time }
        retryable: { type: boolean }
        reason_code: { $ref: "#/components/schemas/ContentSyncReasonCode" }
    ArchiveIngestQueuedReceipt:
      type: object
      additionalProperties: false
      required:
        [
          schema_version,
          request_id,
          idempotency_key,
          project_id,
          change_key,
          archive_id,
          package_sha256,
          manifest_sha256,
          project_version,
          stored_at,
          retryable,
          archive_status,
          change_index_status,
          knowledge_extraction_status,
          change_projection_job_id,
          knowledge_extraction_job_id,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        request_id: { type: string, pattern: "^archive_request:[a-f0-9]{64}$" }
        idempotency_key: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        project_id:
          { type: string, pattern: "^prj_[A-Za-z0-9][A-Za-z0-9._:-]{0,155}$" }
        change_key: { type: string, minLength: 1, maxLength: 160 }
        archive_id:
          { type: string, pattern: "^arc_[A-Za-z0-9][A-Za-z0-9._:-]{0,155}$" }
        package_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        manifest_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        project_version:
          { type: string, pattern: "^pv_[A-Za-z0-9][A-Za-z0-9._:-]{0,155}$" }
        stored_at: { type: string, format: date-time }
        retryable: { type: boolean, const: false }
        archive_status:
          { $ref: "#/components/schemas/ArchiveIngestStoredStatus" }
        change_index_status:
          oneOf:
            - { $ref: "#/components/schemas/ArchiveIngestQueuedStatus" }
            - { $ref: "#/components/schemas/ArchiveIngestIndexingStatus" }
            - { $ref: "#/components/schemas/ArchiveIngestReadyStatus" }
            - { $ref: "#/components/schemas/ArchiveIngestJobFailedStatus" }
        knowledge_extraction_status:
          oneOf:
            - { $ref: "#/components/schemas/ArchiveIngestQueuedStatus" }
            - { $ref: "#/components/schemas/ArchiveIngestExtractingStatus" }
            - { $ref: "#/components/schemas/ArchiveIngestReadyStatus" }
            - { $ref: "#/components/schemas/ArchiveIngestJobFailedStatus" }
        change_projection_job_id:
          { type: string, pattern: "^job_change_[A-Za-z0-9._:-]+$" }
        knowledge_extraction_job_id:
          { type: string, pattern: "^job_knowledge_[A-Za-z0-9._:-]+$" }
        project_content_job_id:
          { type: string, pattern: "^job_content_[A-Za-z0-9._:-]+$" }
    ArchiveIngestPlanningFailedReceipt:
      type: object
      additionalProperties: false
      required:
        [
          schema_version,
          request_id,
          idempotency_key,
          project_id,
          change_key,
          archive_id,
          package_sha256,
          manifest_sha256,
          project_version,
          stored_at,
          retryable,
          archive_status,
          change_index_status,
          knowledge_extraction_status,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        request_id: { type: string, pattern: "^archive_request:[a-f0-9]{64}$" }
        idempotency_key: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        project_id:
          { type: string, pattern: "^prj_[A-Za-z0-9][A-Za-z0-9._:-]{0,155}$" }
        change_key: { type: string, minLength: 1, maxLength: 160 }
        archive_id:
          { type: string, pattern: "^arc_[A-Za-z0-9][A-Za-z0-9._:-]{0,155}$" }
        package_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        manifest_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        project_version:
          { type: string, pattern: "^pv_[A-Za-z0-9][A-Za-z0-9._:-]{0,155}$" }
        stored_at: { type: string, format: date-time }
        retryable: { type: boolean, const: false }
        archive_status:
          { $ref: "#/components/schemas/ArchiveIngestStoredStatus" }
        change_index_status:
          { $ref: "#/components/schemas/ArchiveIngestFailedStatus" }
        knowledge_extraction_status:
          { $ref: "#/components/schemas/ArchiveIngestFailedStatus" }
    CandidateProvenance:
      type: object
      additionalProperties: false
      required:
        [source_kind, source_ref, producer, producer_version, created_at]
      properties:
        source_kind:
          { $ref: "#/components/schemas/CandidateProvenanceSourceKind" }
        source_ref: { type: string, minLength: 1 }
        producer: { type: string, minLength: 1 }
        producer_version: { type: string, minLength: 1 }
        created_at: { type: string, format: date-time }
    ProjectContentCandidate:
      type: object
      additionalProperties: false
      description: Project-content governance candidate in the pcc_ namespace; it is not a knowledge candidate.
      required:
        - schema_version
        - candidate_id
        - candidate_type
        - source_change_key
        - evidence_refs
        - rationale
        - proposed_content
        - content_hash
        - confidence
        - status
        - provenance
      properties:
        schema_version: { type: integer, const: 1 }
        candidate_id:
          type: string
          pattern: "^pcc_[A-Za-z0-9][A-Za-z0-9_-]{0,155}$"
        candidate_type:
          { $ref: "#/components/schemas/ProjectContentCandidateType" }
        source_change_key: { type: string, minLength: 1, maxLength: 160 }
        evidence_refs:
          type: array
          items: { type: string, minLength: 1 }
        rationale: { type: string, minLength: 1 }
        proposed_content: { type: string, minLength: 1 }
        content_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        confidence: { type: number, minimum: 0, maximum: 1 }
        status: { $ref: "#/components/schemas/ProjectContentCandidateStatus" }
        provenance: { $ref: "#/components/schemas/CandidateProvenance" }
    KnowledgeCandidate:
      type: object
      additionalProperties: false
      description: Knowledge-extraction candidate in the kc_ namespace; it is not project content.
      required:
        - schema_version
        - candidate_id
        - source_change_key
        - source_refs
        - summary
        - reusability_scope
        - content_hash
        - confidence
        - status
        - provenance
      properties:
        schema_version: { type: integer, const: 1 }
        candidate_id:
          type: string
          pattern: "^kc_[A-Za-z0-9][A-Za-z0-9_-]{0,155}$"
        source_change_key: { type: string, minLength: 1, maxLength: 160 }
        source_refs:
          type: array
          items: { type: string, minLength: 1 }
        summary: { type: string, minLength: 1 }
        reusability_scope: { type: string, minLength: 1 }
        content_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        confidence: { type: number, minimum: 0, maximum: 1 }
        status: { $ref: "#/components/schemas/KnowledgeCandidateStatus" }
        provenance: { $ref: "#/components/schemas/CandidateProvenance" }
    RemoteVersionIdentity:
      type: object
      additionalProperties: false
      description: Immutable identity recorded for a successfully uploaded remote snapshot version.
      required:
        - project_id
        - branch_name
        - commit_sha
        - artifact_id
        - project_version
        - uploaded_at
        - client_id
        - manifest_hash
      properties:
        project_id: { type: string, pattern: "^prj_" }
        branch_name: { type: string, minLength: 1 }
        commit_sha: { type: string, minLength: 1 }
        change_key: { type: string, minLength: 1, maxLength: 160 }
        artifact_id: { type: string, pattern: "^art_" }
        project_version: { type: string, pattern: "^pv_" }
        uploaded_at: { type: string, format: date-time }
        client_id: { type: string, pattern: "^cli_" }
        manifest_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    BranchSnapshot:
      type: object
      additionalProperties: false
      required:
        - project_id
        - branch_name
        - latest_version
        - artifact_id
        - manifest_hash
        - file_count
        - changed_count
        - uploaded_at
      properties:
        project_id: { type: string, pattern: "^prj_" }
        branch_name: { type: string, minLength: 1 }
        latest_version: { type: string, pattern: "^pv_" }
        commit_sha: { type: string, minLength: 1 }
        artifact_id: { type: string, pattern: "^art_" }
        manifest_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        file_count: { type: integer, minimum: 0, maximum: 9007199254740991 }
        changed_count: { type: integer, minimum: 0, maximum: 9007199254740991 }
        uploaded_at: { type: string, format: date-time }
    SnapshotVersion:
      type: object
      additionalProperties: false
      required:
        [branch_name, project_version, artifact_id, manifest_hash, uploaded_at]
      properties:
        branch_name: { type: string, minLength: 1 }
        project_version: { type: string, pattern: "^pv_" }
        commit_sha: { type: string, minLength: 1 }
        artifact_id: { type: string, pattern: "^art_" }
        manifest_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        uploaded_at: { type: string, format: date-time }
    SnapshotFile:
      type: object
      additionalProperties: false
      required: [path, content_kind, size, content_hash]
      properties:
        path: { type: string, minLength: 1 }
        content_kind: { $ref: "#/components/schemas/ContentKind" }
        size: { type: integer, minimum: 0, maximum: 9007199254740991 }
        content_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        action: { $ref: "#/components/schemas/SyncAction" }
    BranchSnapshotPage:
      type: object
      additionalProperties: false
      description: Items are sorted by uploaded_at descending, then branch_name ascending as the unique tie-breaker.
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/BranchSnapshot" }
        next_cursor:
          type: string
          minLength: 1
          description: An opaque continuation token; return it unchanged and do not infer ordering from it. Omitted on the terminal page.
    SnapshotVersionPage:
      type: object
      additionalProperties: false
      description: Items are sorted by uploaded_at descending, then artifact_id ascending as the unique tie-breaker.
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/SnapshotVersion" }
        next_cursor:
          type: string
          minLength: 1
          description: An opaque continuation token; return it unchanged and do not infer ordering from it. Omitted on the terminal page.
    SnapshotFilePage:
      type: object
      additionalProperties: false
      description: Items are sorted by path ascending as the unique tie-breaker within an immutable snapshot.
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/SnapshotFile" }
        next_cursor:
          type: string
          minLength: 1
          description: An opaque continuation token; return it unchanged and do not infer ordering from it. Omitted on the terminal page.
    LegacyArchiveCompatibilityResult:
      type: object
      additionalProperties: false
      description: Compatibility view for ArchivePackageReceipt. Only durable archive storage is provable; legacy knowledge_status does not imply any split status.
      required:
        - schema_version
        - source_format
        - complete_v1_statuses
        - archive_status
        - change_index_status
        - knowledge_extraction_status
        - managed_snapshot_status
      properties:
        schema_version: { type: integer, const: 1 }
        source_format: { type: string, const: legacy_archive_package_receipt }
        complete_v1_statuses: { type: boolean, const: false }
        archive_status:
          type: object
          additionalProperties: false
          required: [availability, value]
          properties:
            availability: { type: string, const: available }
            value: { $ref: "#/components/schemas/ArchiveStatus" }
        change_index_status:
          type: object
          additionalProperties: false
          required: [availability, reason_code]
          properties:
            availability: { type: string, const: unavailable }
            reason_code:
              { type: string, const: LEGACY_CHANGE_INDEX_STATUS_UNAVAILABLE }
        knowledge_extraction_status:
          type: object
          additionalProperties: false
          required: [availability, reason_code]
          properties:
            availability: { type: string, const: unavailable }
            reason_code:
              type: string
              const: LEGACY_KNOWLEDGE_EXTRACTION_STATUS_UNAVAILABLE
        managed_snapshot_status:
          type: object
          additionalProperties: false
          required: [availability, reason_code]
          properties:
            availability: { type: string, const: unavailable }
            reason_code:
              {
                type: string,
                const: LEGACY_MANAGED_SNAPSHOT_STATUS_UNAVAILABLE,
              }
    InstructionProposalRequest:
      type: object
      additionalProperties: false
      required:
        [
          schema_version,
          project_profile,
          adapters,
          documents,
          codebase_map,
          recent_changes,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        language: { type: string, const: zh-CN, default: zh-CN }
        project_profile: { type: string, minLength: 1, maxLength: 100 }
        adapters:
          type: array
          maxItems: 4
          items: { type: string, enum: [codex, claude-code, cursor, codebuddy] }
        documents:
          type: array
          maxItems: 16
          items:
            type: object
            additionalProperties: false
            required: [path, content, content_sha256]
            properties:
              path:
                type: string
                enum:
                  - AGENTS.md
                  - CLAUDE.md
                  - CODEBUDDY.md
                  - .harness/rules/project-guidance.md
                  - .cursor/rules/project-guidance.mdc
                  - package.json
                  - pom.xml
                  - build.gradle
                  - build.gradle.kts
                  - pyproject.toml
              content: { type: string, maxLength: 524288 }
              content_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        codebase_map:
          type: object
          additionalProperties: false
          required: [status, content]
          properties:
            status: { type: string, enum: [missing, stale, fresh] }
            content: { type: string, maxLength: 524288 }
        recent_changes:
          type: array
          maxItems: 20
          description: Optional client hints; server-owned archive summaries are merged first.
          items:
            type: object
            additionalProperties: false
            required: [change_key, summary, decisions]
            properties:
              change_key: { type: string, minLength: 1, maxLength: 160 }
              summary: { type: string, maxLength: 10000 }
              decisions:
                type: array
                maxItems: 50
                items: { type: string, minLength: 1, maxLength: 2000 }
    InstructionProposal:
      type: object
      additionalProperties: false
      required:
        [
          schema_version,
          proposal_id,
          project_id,
          language,
          mode,
          applied,
          generated_at,
          findings,
          files,
          rule_candidates,
          basis,
          request_id,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        proposal_id:
          { type: string, pattern: "^ipr_[A-Za-z0-9][A-Za-z0-9_-]{0,155}$" }
        project_id: { type: string, pattern: "^prj_" }
        language: { type: string, const: zh-CN }
        mode: { type: string, const: audit-propose }
        applied: { type: boolean, const: false }
        generated_at: { type: string, format: date-time }
        findings:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [code, severity, path, message]
            properties:
              code: { type: string }
              severity: { type: string, enum: [info, warning] }
              path: { type: string }
              message: { type: string }
        files:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              [path, operation, base_content_sha256, content_sha256, content]
            properties:
              path:
                type: string
                enum:
                  - AGENTS.md
                  - CLAUDE.md
                  - CODEBUDDY.md
                  - .harness/rules/project-guidance.md
                  - .cursor/rules/project-guidance.mdc
              operation: { type: string, enum: [add, modify] }
              base_content_sha256:
                type: [string, "null"]
                pattern: "^sha256:[a-f0-9]{64}$"
              content_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
              content: { type: string, maxLength: 1048576 }
        rule_candidates:
          type: array
          description: Evidence-backed candidates with auto_apply=false; not part of apply operations.
          items:
            type: object
            additionalProperties: false
            required:
              [
                candidate_id,
                content,
                evidence,
                evidence_count,
                auto_apply,
                recommendation,
              ]
            properties:
              candidate_id: { type: string, pattern: "^rc_[a-f0-9]{16}$" }
              content: { type: string }
              evidence:
                type: array
                items:
                  type: object
                  additionalProperties: false
                  required: [change_key, summary]
                  properties:
                    change_key: { type: string }
                    summary: { type: string }
              evidence_count: { type: integer, minimum: 1 }
              auto_apply: { type: boolean, const: false }
              recommendation: { type: string, enum: [review, promote] }
        basis: { type: array, items: { type: string, format: uri } }
        request_id: { type: string, format: uuid }
    KnowledgeQueryHttpBudget:
      type: object
      additionalProperties: false
      required: [max_results, max_total_summary_bytes, deadline_ms]
      properties:
        max_results: { type: integer, minimum: 1, maximum: 10 }
        max_total_summary_bytes: { type: integer, minimum: 1, maximum: 65536 }
        deadline_ms: { type: integer, minimum: 1, maximum: 60000 }
    KnowledgeQueryHttpRequest:
      type: object
      additionalProperties: false
      required: [schema_version, project_id, query_id, query_hash, reason_code, query, budget]
      properties:
        schema_version: { type: integer, const: 1 }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,124}$" }
        query_id: { type: string, pattern: "^knowledge_query:[a-f0-9]{64}$" }
        query_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        reason_code: { type: string, enum: [initial_intent, directed_evidence_followup] }
        query: { type: string, minLength: 1, maxLength: 4096 }
        budget: { $ref: "#/components/schemas/KnowledgeQueryHttpBudget" }
    KnowledgeQueryHttpResult:
      type: object
      additionalProperties: false
      required: [result_id, kind, summary, relevance, conflicts_with_intent]
      properties:
        result_id: { type: string, minLength: 1, maxLength: 256 }
        kind: { type: string, enum: [archive_knowledge, implementation_fact, design, rule, change_document] }
        summary: { type: string, minLength: 1, maxLength: 1024 }
        relevance: { type: string, enum: [high, medium, low] }
        source: { type: string, minLength: 1, maxLength: 256 }
        verified_at: { type: string, format: date-time }
        source_version: { type: string, minLength: 1, maxLength: 128 }
        conflicts_with_intent: { type: boolean }
        conflict_summary: { type: string, minLength: 1, maxLength: 1024 }
    KnowledgeQueryHttpReceipt:
      type: object
      additionalProperties: false
      required: [schema_version, receipt_id, query_hash, project_id, result_ids, source_versions, result_set_hash, status, executed_at, reason_code]
      properties:
        schema_version: { type: integer, const: 1 }
        receipt_id: { type: string, pattern: "^knowledge_query_receipt:[a-f0-9]{64}$" }
        query_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,124}$" }
        index_generation: { type: string, minLength: 1, maxLength: 128 }
        result_ids:
          type: array
          maxItems: 10
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 256 }
        source_versions:
          type: array
          maxItems: 10
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 128 }
        result_set_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        status: { type: string, enum: [succeeded, failed] }
        executed_at: { type: string, format: date-time }
        reason_code: { type: string, enum: [initial_intent, directed_evidence_followup, remote_knowledge_unavailable] }
        failure_code: { type: string, minLength: 1, maxLength: 128 }
        supersedes: { type: string, pattern: "^knowledge_query_receipt:[a-f0-9]{64}$" }
    KnowledgeQueryHttpResponse:
      type: object
      additionalProperties: false
      required: [schema_version, query_id, project_id, receipt, results]
      properties:
        schema_version: { type: integer, const: 1 }
        query_id: { type: string, pattern: "^knowledge_query:[a-f0-9]{64}$" }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,124}$" }
        receipt: { $ref: "#/components/schemas/KnowledgeQueryHttpReceipt" }
        results:
          type: array
          maxItems: 10
          items: { $ref: "#/components/schemas/KnowledgeQueryHttpResult" }
    PlatformInformationView:
      type: string
      enum:
        [
          branch_monitor,
          branch_files,
          project_materials,
          project_knowledge,
          change_records,
          version_records,
        ]
    PlatformInformationPageState:
      type: string
      enum: [ready, empty, processing, partial_failure, failed, forbidden]
    PlatformInformationContentType:
      type: string
      enum:
        [
          run_event,
          branch_file,
          config,
          rule,
          architecture,
          instruction,
          knowledge_entry,
          change_document,
          archive_package,
          project_content_candidate,
        ]
    PlatformInformationSort:
      type: string
      description: Stable, view-owned ordering. The final field is the unique tie-breaker.
      enum:
        [
          last_event_at_desc_run_id_asc,
          uploaded_at_desc_snapshot_version_asc,
          category_asc_path_asc_version_desc,
          extracted_at_desc_knowledge_id_asc,
          archived_at_desc_change_key_asc,
        ]
    PlatformInformationPath:
      type: string
      minLength: 1
      maxLength: 1024
      pattern: "^(?!/)(?!.*\\\\)(?!.*(?:^|/)(?:\\.|\\.\\.)(?:/|$))(?!.*//).+$"
    PlatformInformationQueryScope:
      type: object
      additionalProperties: false
      description: Trusted server Adapter input. The route Adapter derives this scope; clients cannot grant access by submitting it.
      required: [actor_id, accessible_project_ids, content_types]
      properties:
        actor_id: { type: string, minLength: 1, maxLength: 160 }
        accessible_project_ids:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        content_types:
          type: array
          minItems: 1
          maxItems: 10
          uniqueItems: true
          items: { $ref: "#/components/schemas/PlatformInformationContentType" }
    PlatformInformationListHttpQuery:
      type: object
      additionalProperties: false
      description: Public query only; actor, project allowlist, content types, sort and cursor authority are Server-derived.
      properties:
        limit: { type: integer, minimum: 1, maximum: 100, default: 50 }
        cursor: { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$", default: null }
    PlatformInformationExportCreateHttpRequest:
      $ref: "#/components/schemas/PlatformInformationListHttpQuery"
    PlatformInformationRetryExtractionHttpRequest:
      type: object
      additionalProperties: false
      required: [job_id, expected_generation]
      description: Actor and project identity are derived from authentication and the path.
      properties:
        job_id:
          { type: string, pattern: "^job_knowledge_[A-Za-z0-9._:-]{1,146}$" }
        expected_generation: { type: integer, minimum: 0 }
    PlatformInformationConfirmRestoreHttpRequest:
      type: object
      additionalProperties: false
      required: [preview_receipt, confirmation_intent]
      properties:
        preview_receipt:
          { $ref: "#/components/schemas/RestoreBranchFilesPreviewReceipt" }
        confirmation_intent:
          { $ref: "#/components/schemas/RestoreBranchFilesConfirmationIntent" }
    RestoreBranchFilesConfirmedIntent:
      type: object
      additionalProperties: false
      description: Request-only, preview-validated output consumed by the local Pull Adapter; it grants no Platform write capability.
      required:
        [
          schema_version,
          contract_kind,
          project_id,
          source_ref,
          source_version,
          scopes,
          selected_paths,
          preview_hash,
          idempotency_key,
          conflict_decisions,
          request_only,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind:
          { type: string, const: branch_files_pull_confirmed_intent }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        source_ref: { $ref: "#/components/schemas/RestoreBranchFilesSourceRef" }
        source_version:
          { $ref: "#/components/schemas/RestoreBranchFilesSourceVersion" }
        scopes:
          {
            type: array,
            prefixItems: [{ type: string, const: branch_files }],
            minItems: 1,
            maxItems: 1,
          }
        selected_paths:
          {
            type: array,
            minItems: 1,
            maxItems: 1000,
            uniqueItems: true,
            items: { $ref: "#/components/schemas/PlatformInformationPath" },
          }
        preview_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        idempotency_key: { type: string, minLength: 1, maxLength: 160 }
        conflict_decisions:
          type: array
          maxItems: 1000
          items:
            type: object
            additionalProperties: false
            required:
              [
                path,
                resolution,
                expected_preview_hash,
                source_artifact_id,
                source_project_version,
              ]
            properties:
              path: { $ref: "#/components/schemas/PlatformInformationPath" }
              resolution:
                { type: string, enum: [keep_local, accept_remote, skip] }
              expected_preview_hash:
                { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
              source_artifact_id: { type: string, minLength: 1, maxLength: 160 }
              source_project_version:
                { type: string, minLength: 1, maxLength: 160 }
        request_only: { type: boolean, const: true }
    PlatformInformationQuery:
      type: object
      additionalProperties: false
      description: Bounded storage query. A syntactically bounded opaque token is not trusted until PlatformInformationCursorVerifierPort verifies its actor, project, view and sort binding.
      x-hunter-cross-field-invariants:
        - query_scope.accessible_project_ids contains project_id
        - view fixes the exact query_scope.content_types sequence and sort value
      required:
        [
          schema_version,
          contract_kind,
          view,
          project_id,
          query_scope,
          limit,
          cursor,
          cursor_verification,
          sort,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind: { type: string, const: query }
        view: { $ref: "#/components/schemas/PlatformInformationView" }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        query_scope:
          { $ref: "#/components/schemas/PlatformInformationQueryScope" }
        limit: { type: integer, minimum: 1, maximum: 100 }
        cursor:
          type: [string, "null"]
          pattern: "^[A-Za-z0-9_-]{16,512}$"
          description: Syntactically bounded opaque continuation token. Consumers return it unchanged; the server Port must verify authenticity and query binding.
        cursor_verification: { type: string, const: server_port_required }
        sort: { $ref: "#/components/schemas/PlatformInformationSort" }
    PlatformInformationBlobRef:
      type: object
      additionalProperties: false
      required: [blob_hash, snapshot_version]
      properties:
        blob_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        snapshot_version: { type: string, minLength: 1, maxLength: 160 }
    PlatformInformationArchiveDownloadRef:
      type: object
      additionalProperties: false
      description: Independent archive-package download identity; it is not a knowledge or document reference.
      required: [archive_id, package_hash]
      properties:
        archive_id: { type: string, minLength: 1, maxLength: 160 }
        package_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    PlatformInformationBranchMonitorItem:
      type: object
      additionalProperties: false
      required:
        [
          item_kind,
          lifecycle_kind,
          run_id,
          branch_name,
          change_key,
          run_status,
          current_phase,
          started_at,
          ended_at,
          duration_ms,
          last_event_at,
          sort_key,
        ]
      properties:
        item_kind: { type: string, const: branch_monitor }
        lifecycle_kind:
          {
            type: string,
            const: change,
            description: Sync is excluded from monitoring.,
          }
        run_id: { type: string, minLength: 1, maxLength: 160 }
        branch_name: { type: string, minLength: 1, maxLength: 512 }
        change_key: { type: string, minLength: 1, maxLength: 160 }
        run_status:
          { type: string, enum: [running, succeeded, failed, partial] }
        current_phase: { type: [string, "null"], minLength: 1, maxLength: 80 }
        started_at: { type: string, format: date-time }
        ended_at: { type: [string, "null"], format: date-time }
        duration_ms: { type: [integer, "null"], minimum: 0 }
        last_event_at: { type: string, format: date-time }
        sort_key: { type: string, minLength: 1, maxLength: 512 }
    PlatformInformationBranchSnapshotItem:
      type: object
      additionalProperties: false
      required:
        [
          item_kind,
          branch_name,
          snapshot_version,
          commit_sha,
          uploaded_at,
          file_count,
          changed_file_count,
          sort_key,
        ]
      properties:
        item_kind: { type: string, const: branch_snapshot }
        branch_name: { type: string, minLength: 1, maxLength: 160 }
        snapshot_version: { type: string, minLength: 1, maxLength: 160 }
        commit_sha: { type: string, pattern: "^[a-f0-9]{40,64}$" }
        uploaded_at: { type: string, format: date-time }
        file_count: { type: integer, minimum: 0 }
        changed_file_count: { type: integer, minimum: 0 }
        sort_key: { type: string, minLength: 1, maxLength: 512 }
    PlatformInformationProjectMaterialItem:
      type: object
      additionalProperties: false
      description: A categorized projection referencing the snapshot Blob; it never duplicates the body.
      required:
        [
          item_kind,
          material_id,
          category,
          path,
          blob_ref,
          source_branch_name,
          source_commit_sha,
          sort_key,
        ]
      properties:
        item_kind: { type: string, const: project_material }
        material_id: { type: string, minLength: 1, maxLength: 160 }
        category:
          {
            type: string,
            enum:
              [
                config,
                rule,
                architecture_map,
                architecture_constraint,
                instruction,
              ],
          }
        path: { $ref: "#/components/schemas/PlatformInformationPath" }
        blob_ref: { $ref: "#/components/schemas/PlatformInformationBlobRef" }
        source_branch_name: { type: string, minLength: 1, maxLength: 160 }
        source_commit_sha: { type: string, pattern: "^[a-f0-9]{40,64}$" }
        sort_key: { type: string, minLength: 1, maxLength: 512 }
    PlatformInformationKnowledgeItem:
      type: object
      additionalProperties: false
      description: Reusable knowledge only; rules, architecture, plans, designs and change documents are excluded by query scope.
      required:
        [
          item_kind,
          knowledge_id,
          display_title,
          lifecycle_status,
          source_change_key,
          extracted_at,
          relationship_count,
          sort_key,
        ]
      properties:
        item_kind: { type: string, const: knowledge_entry }
        knowledge_id: { type: string, minLength: 1, maxLength: 160 }
        display_title:
          {
            type: string,
            minLength: 1,
            maxLength: 240,
            description: Explicit producer-owned title; consumers must not infer it from summary or body.,
          }
        lifecycle_status:
          {
            type: string,
            enum:
              [candidate, active, stale, deprecated, superseded, conflicted],
          }
        source_change_key: { type: string, minLength: 1, maxLength: 160 }
        extracted_at: { type: string, format: date-time }
        relationship_count: { type: integer, minimum: 0 }
        sort_key: { type: string, minLength: 1, maxLength: 512 }
    PlatformInformationChangeRecordItem:
      type: object
      additionalProperties: false
      required:
        [
          item_kind,
          change_key,
          title,
          archived_at,
          archive_status,
          knowledge_extraction_status,
          document_refs,
          candidate_count,
          archive_download_ref,
          sort_key,
        ]
      properties:
        item_kind: { type: string, const: change_record }
        change_key: { type: string, minLength: 1, maxLength: 160 }
        title: { type: string, minLength: 1, maxLength: 240 }
        archived_at: { type: string, format: date-time }
        archive_status:
          { type: string, enum: [absent, uploading, stored, failed] }
        knowledge_extraction_status:
          {
            type: string,
            enum: [not_scheduled, queued, extracting, ready, failed],
          }
        document_refs:
          {
            type: array,
            maxItems: 20,
            items: { type: string, minLength: 1, maxLength: 160 },
          }
        candidate_count: { type: integer, minimum: 0 }
        archive_download_ref:
          oneOf:
            - {
                $ref: "#/components/schemas/PlatformInformationArchiveDownloadRef",
              }
            - { type: "null" }
        sort_key: { type: string, minLength: 1, maxLength: 512 }
    PlatformInformationVersionRecordItem:
      type: object
      additionalProperties: false
      required:
        [
          item_kind,
          snapshot_version,
          branch_name,
          commit_sha,
          uploaded_at,
          file_count,
          changed_file_count,
          diff_ref,
          sort_key,
        ]
      properties:
        item_kind: { type: string, const: version_record }
        snapshot_version: { type: string, minLength: 1, maxLength: 160 }
        branch_name: { type: string, minLength: 1, maxLength: 160 }
        commit_sha: { type: string, pattern: "^[a-f0-9]{40,64}$" }
        uploaded_at: { type: string, format: date-time }
        file_count: { type: integer, minimum: 0 }
        changed_file_count: { type: integer, minimum: 0 }
        diff_ref: { type: string, minLength: 1, maxLength: 160 }
        sort_key: { type: string, minLength: 1, maxLength: 512 }
    PlatformInformationItem:
      oneOf:
        - { $ref: "#/components/schemas/PlatformInformationBranchMonitorItem" }
        - { $ref: "#/components/schemas/PlatformInformationBranchSnapshotItem" }
        - {
            $ref: "#/components/schemas/PlatformInformationProjectMaterialItem",
          }
        - { $ref: "#/components/schemas/PlatformInformationKnowledgeItem" }
        - { $ref: "#/components/schemas/PlatformInformationChangeRecordItem" }
        - { $ref: "#/components/schemas/PlatformInformationVersionRecordItem" }
      discriminator: { propertyName: item_kind }
    PlatformInformationFailure:
      type: object
      additionalProperties: false
      required: [reason_code, retryable]
      properties:
        reason_code:
          {
            type: string,
            enum:
              [
                PROJECT_INFORMATION_FORBIDDEN,
                PROJECTION_PARTIAL_FAILURE,
                KNOWLEDGE_EXTRACTION_FAILED,
              ],
          }
        retryable: { type: boolean }
    PlatformInformationPage:
      type: object
      additionalProperties: false
      description: Lists contain summaries and references only; body/content is loaded through PlatformInformationDetailResponse.
      oneOf:
        - properties:
            page_state: { type: string, const: ready }
            items: { type: array, minItems: 1, maxItems: 100 }
            failures: { type: array, maxItems: 0 }
        - properties:
            page_state: { type: string, const: empty }
            items: { type: array, maxItems: 0 }
            next_cursor: { type: "null" }
            failures: { type: array, maxItems: 0 }
        - properties:
            page_state: { type: string, const: processing }
            items: { type: array, maxItems: 0 }
            next_cursor: { type: "null" }
            failures: { type: array, maxItems: 0 }
        - properties:
            page_state: { type: string, const: partial_failure }
            items: { type: array, minItems: 1, maxItems: 100 }
            failures:
              type: array
              minItems: 1
              maxItems: 10
              items:
                type: object
                additionalProperties: false
                required: [reason_code, retryable]
                properties:
                  reason_code:
                    { type: string, const: PROJECTION_PARTIAL_FAILURE }
                  retryable: { type: boolean }
        - properties:
            view: { type: string, const: project_knowledge }
            page_state: { type: string, const: failed }
            items: { type: array, maxItems: 0 }
            next_cursor: { type: "null" }
            failures:
              type: array
              minItems: 1
              maxItems: 1
              items:
                type: object
                additionalProperties: false
                required: [reason_code, retryable]
                properties:
                  reason_code:
                    { type: string, const: KNOWLEDGE_EXTRACTION_FAILED }
                  retryable: { type: boolean, const: true }
        - properties:
            page_state: { type: string, const: forbidden }
            items: { type: array, maxItems: 0 }
            next_cursor: { type: "null" }
            failures:
              type: array
              minItems: 1
              maxItems: 1
              items:
                type: object
                additionalProperties: false
                required: [reason_code, retryable]
                properties:
                  reason_code:
                    { type: string, const: PROJECT_INFORMATION_FORBIDDEN }
                  retryable: { type: boolean }
      required:
        [
          schema_version,
          contract_kind,
          view,
          project_id,
          page_state,
          sort,
          items,
          next_cursor,
          failures,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind: { type: string, const: page }
        view: { $ref: "#/components/schemas/PlatformInformationView" }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        page_state:
          { $ref: "#/components/schemas/PlatformInformationPageState" }
        sort: { $ref: "#/components/schemas/PlatformInformationSort" }
        items:
          {
            type: array,
            maxItems: 100,
            items: { $ref: "#/components/schemas/PlatformInformationItem" },
          }
        next_cursor:
          { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$" }
        failures:
          {
            type: array,
            maxItems: 10,
            items: { $ref: "#/components/schemas/PlatformInformationFailure" },
          }
    PlatformInformationDetailRequest:
      type: object
      additionalProperties: false
      required:
        [
          schema_version,
          contract_kind,
          view,
          project_id,
          query_scope,
          detail_id,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind: { type: string, const: detail_request }
        view: { $ref: "#/components/schemas/PlatformInformationView" }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        query_scope:
          { $ref: "#/components/schemas/PlatformInformationQueryScope" }
        detail_id: { type: string, minLength: 1, maxLength: 160 }
    PlatformInformationContentDetail:
      type: object
      additionalProperties: false
      required: [detail_kind, content, content_hash, media_type]
      properties:
        detail_kind:
          {
            type: string,
            enum:
              [branch_file, project_material, knowledge_entry, change_document],
          }
        content: { type: string, maxLength: 2000000 }
        content_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        media_type:
          {
            type: string,
            enum:
              [text/plain, text/markdown, application/json, application/yaml],
          }
    PlatformInformationMonitorDetail:
      type: object
      additionalProperties: false
      required: [detail_kind, lifecycle_kind, event_refs]
      properties:
        detail_kind: { type: string, const: branch_monitor }
        lifecycle_kind: { type: string, const: change }
        event_refs:
          {
            type: array,
            maxItems: 100,
            items: { type: string, minLength: 1, maxLength: 160 },
          }
    PlatformInformationChangeDetail:
      type: object
      additionalProperties: false
      required:
        [detail_kind, document_refs, candidate_refs, archive_download_ref]
      properties:
        detail_kind: { type: string, const: change_record }
        document_refs:
          {
            type: array,
            maxItems: 20,
            items: { type: string, minLength: 1, maxLength: 160 },
          }
        candidate_refs:
          {
            type: array,
            maxItems: 100,
            items: { type: string, minLength: 1, maxLength: 160 },
          }
        archive_download_ref:
          oneOf:
            - {
                $ref: "#/components/schemas/PlatformInformationArchiveDownloadRef",
              }
            - { type: "null" }
    PlatformInformationVersionDiffDetail:
      type: object
      additionalProperties: false
      required: [detail_kind, from_version, to_version, changed_paths]
      properties:
        detail_kind: { type: string, const: version_diff }
        from_version: { type: string, minLength: 1, maxLength: 160 }
        to_version: { type: string, minLength: 1, maxLength: 160 }
        changed_paths:
          {
            type: array,
            maxItems: 1000,
            items: { $ref: "#/components/schemas/PlatformInformationPath" },
          }
    PlatformInformationDetailResponse:
      type: object
      additionalProperties: false
      required:
        [schema_version, contract_kind, view, project_id, detail_id, detail]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind: { type: string, const: detail_response }
        view: { $ref: "#/components/schemas/PlatformInformationView" }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        detail_id: { type: string, minLength: 1, maxLength: 160 }
        detail:
          description: View-specific detail payload. Text bodies are permitted only at this detail seam.
          oneOf:
            - { $ref: "#/components/schemas/PlatformInformationContentDetail" }
            - { $ref: "#/components/schemas/PlatformInformationMonitorDetail" }
            - { $ref: "#/components/schemas/PlatformInformationChangeDetail" }
            - {
                $ref: "#/components/schemas/PlatformInformationVersionDiffDetail",
              }
          discriminator: { propertyName: detail_kind }
    KnowledgeExtractionRetryIntent:
      type: object
      additionalProperties: false
      description: Request-only capability for retrying one failed knowledge extraction generation. It grants no execution authority and carries no token or body.
      x-hunter-cross-field-invariants:
        - intent_hash is the stable canonical hash of actor_id, project_id, job_id, expected_generation, retryable and request_only
      required:
        [
          schema_version,
          contract_kind,
          actor_id,
          project_id,
          job_id,
          expected_generation,
          retryable,
          request_only,
          intent_hash,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind:
          { type: string, const: knowledge_extraction_retry_intent }
        actor_id: { type: string, minLength: 1, maxLength: 160 }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        job_id:
          { type: string, pattern: "^job_knowledge_[A-Za-z0-9._:-]{1,146}$" }
        expected_generation: { type: integer, minimum: 0 }
        retryable: { type: boolean, const: true }
        request_only: { type: boolean, const: true }
        intent_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    RestoreBranchFilesIntent:
      type: object
      additionalProperties: false
      description: Request intent for the stage 02/03 pull-preview seam. This contract cannot write project files.
      x-hunter-cross-field-invariants:
        - selected_paths are unique
      required:
        [
          schema_version,
          contract_kind,
          project_id,
          source_branch_name,
          source_commit_sha,
          source_artifact_id,
          source_project_version,
          scopes,
          selected_paths,
          preview_only,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind: { type: string, const: branch_files_pull_preview_intent }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        source_branch_name: { type: string, minLength: 1, maxLength: 160 }
        source_commit_sha: { type: string, pattern: "^[a-f0-9]{40,64}$" }
        source_artifact_id: { type: string, minLength: 1, maxLength: 160 }
        source_project_version: { type: string, minLength: 1, maxLength: 160 }
        scopes:
          {
            type: array,
            prefixItems: [{ type: string, const: branch_files }],
            minItems: 1,
            maxItems: 1,
          }
        selected_paths:
          {
            type: array,
            minItems: 1,
            maxItems: 1000,
            uniqueItems: true,
            items: { $ref: "#/components/schemas/PlatformInformationPath" },
          }
        preview_only: { type: boolean, const: true }
    RestoreBranchFilesSourceRef:
      type: object
      additionalProperties: false
      required: [project_id, branch_name, commit_sha, client_id]
      properties:
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        branch_name: { type: string, minLength: 1, maxLength: 160 }
        commit_sha: { type: string, pattern: "^[a-f0-9]{40,64}$" }
        client_id: { type: string, minLength: 1, maxLength: 160 }
    RestoreBranchFilesSourceVersion:
      type: object
      additionalProperties: false
      required: [branch_name, commit_sha, artifact_id, project_version]
      properties:
        branch_name: { type: string, minLength: 1, maxLength: 160 }
        commit_sha: { type: string, pattern: "^[a-f0-9]{40,64}$" }
        artifact_id: { type: string, minLength: 1, maxLength: 160 }
        project_version: { type: string, minLength: 1, maxLength: 160 }
    RestoreBranchFilesPreviewReceipt:
      type: object
      additionalProperties: false
      description: Read-only stage 02 pull preview projection. It carries no file body and grants no write capability.
      x-hunter-cross-field-invariants:
        - source_ref.project_id equals project_id
        - source_ref.branch_name and commit_sha equal source_version branch_name and commit_sha
        - selected_paths and conflict paths are unique
        - every conflict path is in selected_paths
      required:
        [
          schema_version,
          contract_kind,
          project_id,
          source_ref,
          source_version,
          scopes,
          selected_paths,
          preview_hash,
          conflicts,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind:
          { type: string, const: branch_files_pull_preview_receipt }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        source_ref: { $ref: "#/components/schemas/RestoreBranchFilesSourceRef" }
        source_version:
          { $ref: "#/components/schemas/RestoreBranchFilesSourceVersion" }
        scopes:
          {
            type: array,
            prefixItems: [{ type: string, const: branch_files }],
            minItems: 1,
            maxItems: 1,
          }
        selected_paths:
          {
            type: array,
            minItems: 1,
            maxItems: 1000,
            uniqueItems: true,
            items: { $ref: "#/components/schemas/PlatformInformationPath" },
          }
        preview_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        conflicts:
          type: array
          maxItems: 1000
          items:
            type: object
            additionalProperties: false
            required: [path, reason_code]
            properties:
              path: { $ref: "#/components/schemas/PlatformInformationPath" }
              reason_code:
                {
                  type: string,
                  enum: [SYNC_CONTENT_CONFLICT, SYNC_RENAME_TARGET_CONFLICT],
                }
    RestoreBranchFilesConfirmationIntent:
      type: object
      additionalProperties: false
      description: The only final Platform restore output. The stage 02/03 Pull Adapter consumes it after preview; Platform never writes project files directly.
      oneOf:
        - properties:
            action: { type: string, const: continue }
        - properties:
            action: { type: string, enum: [review, stop] }
            conflict_decisions: { type: array, maxItems: 0 }
      required:
        [
          schema_version,
          contract_kind,
          project_id,
          source_ref,
          source_version,
          scopes,
          preview_hash,
          action,
          idempotency_key,
          conflict_decisions,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind:
          { type: string, const: branch_files_pull_confirmation_intent }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        source_ref: { $ref: "#/components/schemas/RestoreBranchFilesSourceRef" }
        source_version:
          { $ref: "#/components/schemas/RestoreBranchFilesSourceVersion" }
        scopes:
          {
            type: array,
            prefixItems: [{ type: string, const: branch_files }],
            minItems: 1,
            maxItems: 1,
          }
        preview_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        action: { type: string, enum: [continue, review, stop] }
        idempotency_key: { type: string, minLength: 1, maxLength: 160 }
        conflict_decisions:
          type: array
          maxItems: 1000
          items:
            type: object
            additionalProperties: false
            required:
              [
                path,
                resolution,
                expected_preview_hash,
                source_artifact_id,
                source_project_version,
              ]
            properties:
              path: { $ref: "#/components/schemas/PlatformInformationPath" }
              resolution:
                { type: string, enum: [keep_local, accept_remote, skip] }
              expected_preview_hash:
                { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
              source_artifact_id: { type: string, minLength: 1, maxLength: 160 }
              source_project_version:
                { type: string, minLength: 1, maxLength: 160 }
    PlatformInformationExportResult:
      type: object
      additionalProperties: false
      description: Proof of an export-all cursor walk, bound to the complete trusted query range. The first request cursor may be null exactly when the source cursor is null; every subsequent request cursor is unique and non-null.
      x-hunter-cross-field-invariants:
        - first request_cursor equals range.source_cursor
        - range query_scope, limit, cursor_verification and sort equal the trusted source query
        - range content types and sort match view policy and project_id belongs to the actor allowlist
        - subsequent request_cursor values and non-terminal response_next_cursor values are non-null and unique
        - each response_next_cursor equals the next request_cursor
        - the final response_next_cursor is null
        - each result_count is no greater than range.limit
        - exported_count equals the sum of result_count
      required:
        [
          schema_version,
          contract_kind,
          view,
          project_id,
          range,
          pages,
          exported_count,
          completed,
        ]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind: { type: string, const: export_all_result }
        view: { $ref: "#/components/schemas/PlatformInformationView" }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        range:
          type: object
          additionalProperties: false
          required:
            [query_scope, limit, source_cursor, cursor_verification, sort]
          properties:
            query_scope:
              { $ref: "#/components/schemas/PlatformInformationQueryScope" }
            limit: { type: integer, minimum: 1, maximum: 100 }
            source_cursor:
              { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$" }
            cursor_verification: { type: string, const: server_port_required }
            sort: { $ref: "#/components/schemas/PlatformInformationSort" }
        pages:
          type: array
          minItems: 1
          maxItems: 10000
          items:
            type: object
            additionalProperties: false
            required: [request_cursor, response_next_cursor, result_count]
            properties:
              request_cursor:
                { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$" }
              response_next_cursor:
                { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$" }
              result_count: { type: integer, minimum: 0, maximum: 100 }
        exported_count: { type: integer, minimum: 0, maximum: 1000000 }
        completed: { type: boolean, const: true }
    PlatformInformationExportRange:
      type: object
      additionalProperties: false
      required: [query_scope, limit, source_cursor, cursor_verification, sort]
      properties:
        query_scope: { $ref: "#/components/schemas/PlatformInformationQueryScope" }
        limit: { type: integer, minimum: 1, maximum: 100 }
        source_cursor:
          { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$" }
        cursor_verification: { type: string, const: server_port_required }
        sort: { $ref: "#/components/schemas/PlatformInformationSort" }
    PlatformInformationExportPageProof:
      type: object
      additionalProperties: false
      required: [request_cursor, response_next_cursor, result_count]
      properties:
        request_cursor:
          { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$" }
        response_next_cursor:
          { type: [string, "null"], pattern: "^[A-Za-z0-9_-]{16,512}$" }
        result_count: { type: integer, minimum: 0, maximum: 100 }
    PlatformInformationExportM4Proof:
      type: object
      additionalProperties: false
      description: Complete M4 cursor walk plus the canonical item-line accumulator produced during that walk.
      required: [pages, exported_count, items_sha, completed]
      properties:
        pages:
          type: array
          minItems: 1
          maxItems: 10000
          items: { $ref: "#/components/schemas/PlatformInformationExportPageProof" }
        exported_count: { type: integer, minimum: 0, maximum: 1000000 }
        items_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        completed: { type: boolean, const: true }
    PlatformInformationExportArtifactSummary:
      type: object
      additionalProperties: false
      description: content_sha hashes every artifact byte. items_sha hashes the concatenation of each exact canonical item line plus its LF, excluding manifest and footer.
      required:
        [format, media_type, content_sha, items_sha, byte_count, item_count, page_count]
      properties:
        format: { type: string, const: canonical_jsonl_v1 }
        media_type: { type: string, const: application/x-ndjson }
        content_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        items_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        byte_count: { type: integer, minimum: 1, maximum: 536870912 }
        item_count: { type: integer, minimum: 0, maximum: 1000000 }
        page_count: { type: integer, minimum: 1, maximum: 10000 }
    PlatformInformationExportDownloadRef:
      type: object
      additionalProperties: false
      required: [export_id, project_id, content_sha]
      properties:
        export_id: { type: string, minLength: 1, maxLength: 160 }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        content_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    PlatformInformationExportArtifactReceipt:
      type: object
      additionalProperties: false
      description: A bounded ready receipt for a canonical JSONL export artifact. Item bodies remain in the downloadable stream and are never embedded in this receipt.
      x-hunter-cross-field-invariants:
        - m4_proof is a complete PlatformInformationExportResult proof for project_id, view and range
        - artifact item_count and page_count equal the M4 exported count and proof page count; artifact.items_sha equals m4_proof.items_sha
        - download_ref identities equal export_id, project_id and artifact.content_sha
        - proof_sha hashes canonical JSON of schema_version, export_id, project_id, view, range and m4_proof, whose items_sha binds the streamed item lines without a content/proof cycle
        - incremental readers reject empty chunks and chunks larger than 1048576 bytes before decoding
        - expires_at is later than created_at
      required:
        [schema_version, contract_kind, export_id, project_id, view, range, m4_proof, proof_sha, artifact, download_ref, status, created_at, expires_at]
      properties:
        schema_version: { type: integer, const: 1 }
        contract_kind:
          { type: string, const: platform_information_export_artifact_receipt }
        export_id: { type: string, minLength: 1, maxLength: 160 }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        view: { $ref: "#/components/schemas/PlatformInformationView" }
        range: { $ref: "#/components/schemas/PlatformInformationExportRange" }
        m4_proof: { $ref: "#/components/schemas/PlatformInformationExportM4Proof" }
        proof_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        artifact: { $ref: "#/components/schemas/PlatformInformationExportArtifactSummary" }
        download_ref: { $ref: "#/components/schemas/PlatformInformationExportDownloadRef" }
        status: { type: string, const: ready }
        created_at: { type: string, format: date-time }
        expires_at: { type: string, format: date-time }
    PlatformInformationExportManifestLine:
      type: object
      additionalProperties: false
      description: Exactly one first line. UTF-8, canonical JSON, one LF terminator, and no BOM or CR bytes are permitted.
      required:
        [schema_version, line_kind, format, export_id, project_id, view, range, m4_proof, proof_sha, created_at, expires_at]
      properties:
        schema_version: { type: integer, const: 1 }
        line_kind: { type: string, const: manifest }
        format: { type: string, const: canonical_jsonl_v1 }
        export_id: { type: string, minLength: 1, maxLength: 160 }
        project_id: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        view: { $ref: "#/components/schemas/PlatformInformationView" }
        range: { $ref: "#/components/schemas/PlatformInformationExportRange" }
        m4_proof: { $ref: "#/components/schemas/PlatformInformationExportM4Proof" }
        proof_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        created_at: { type: string, format: date-time }
        expires_at: { type: string, format: date-time }
    PlatformInformationExportItemLine:
      type: object
      additionalProperties: false
      description: A one-based contiguous ordinal. item_sha hashes the UTF-8 canonical JSON bytes of item only; the complete canonical line contributes to items_sha.
      required: [schema_version, line_kind, ordinal, item_sha, item]
      properties:
        schema_version: { type: integer, const: 1 }
        line_kind: { type: string, const: item }
        ordinal: { type: integer, minimum: 1, maximum: 1000000 }
        item_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        item: { $ref: "#/components/schemas/PlatformInformationItem" }
    PlatformInformationExportFooterLine:
      type: object
      additionalProperties: false
      description: Exactly one final line, followed by one LF and no further bytes. Hashes and counts must equal the receipt and observed stream.
      required:
        [schema_version, line_kind, export_id, proof_sha, items_sha, item_count, page_count]
      properties:
        schema_version: { type: integer, const: 1 }
        line_kind: { type: string, const: footer }
        export_id: { type: string, minLength: 1, maxLength: 160 }
        proof_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        items_sha: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        item_count: { type: integer, minimum: 0, maximum: 1000000 }
        page_count: { type: integer, minimum: 1, maximum: 10000 }
    PlatformInformationExportJsonlLine:
      description: Canonical JSONL permits only manifest, item and footer lines in that order; every line uses UTF-8 LF framing with no BOM.
      oneOf:
        - { $ref: "#/components/schemas/PlatformInformationExportManifestLine" }
        - { $ref: "#/components/schemas/PlatformInformationExportItemLine" }
        - { $ref: "#/components/schemas/PlatformInformationExportFooterLine" }
      discriminator: { propertyName: line_kind }
    LegacyPlatformInformationExportArtifactReceipt:
      type: object
      additionalProperties: false
      description: Legacy JSON export metadata accepted only for read-only display.
      required: [schemaVersion, exportId, projectId, format, createdAt]
      properties:
        schemaVersion: { type: integer, const: 0 }
        exportId: { type: string, minLength: 1, maxLength: 160 }
        projectId: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        format: { type: string, const: json }
        createdAt: { type: string, format: date-time }
    LegacyPlatformInformation:
      type: object
      additionalProperties: false
      description: Legacy file-page payload accepted for read-only display; it is never normalized into a writable v1 request.
      required: [schemaVersion, projectId, page, items]
      properties:
        schemaVersion: { type: integer, const: 0 }
        projectId: { type: string, pattern: "^prj_[A-Za-z0-9_-]{1,156}$" }
        page: { type: string, const: files }
        items:
          type: array
          maxItems: 1000
          items:
            type: object
            additionalProperties: false
            required: [path, content]
            properties:
              path: { $ref: "#/components/schemas/PlatformInformationPath" }
              content: { type: string }
    SensitiveFindingSummary:
      type: object
      additionalProperties: false
      properties:
        path:
          type: string
        rule_id:
          type: string
        severity:
          type: string
        overridable:
          type: boolean
        fingerprint:
          type: string
        line:
          type: integer
        column:
          type: integer
    RemoteContentUploadHttpSource:
      type: object
      additionalProperties: false
      required: [project_id, branch_name, actor_id]
      properties:
        project_id: { type: string, minLength: 1, maxLength: 160 }
        branch_name: { type: string, minLength: 1, maxLength: 160 }
        actor_id: { type: string, minLength: 1, maxLength: 160 }
        commit_sha: { type: string, minLength: 1, maxLength: 240 }
        client_id: { type: string, minLength: 1, maxLength: 240 }
        change_key: { type: string, minLength: 1, maxLength: 240 }
    RemoteContentUploadHttpRef:
      type: object
      additionalProperties: false
      required: [ref_id, sha256, size_bytes]
      properties:
        ref_id: { type: string, pattern: "^bounded_upload:[A-Za-z0-9_-]{43}$" }
        sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        size_bytes: { type: integer, minimum: 1, maximum: 536870912 }
    RemoteContentUploadHttpRecord:
      type: object
      additionalProperties: false
      x-hunter-cross-field-invariants:
        - upload_id suffix equals upload_ref.ref_id suffix
        - upload_ref sha256 and size_bytes equal content_sha256 and size_bytes
        - record_hash hashes the canonical record body without record_hash
        - expires_at is later than created_at and at most 900000 milliseconds later
      required: [schema_version, upload_id, source, idempotency_key, purpose, content_sha256, size_bytes, upload_ref, state, created_at, expires_at, record_hash]
      properties:
        schema_version: { type: integer, const: 1 }
        upload_id: { type: string, pattern: "^remote_content_upload:[A-Za-z0-9_-]{43}$" }
        source: { $ref: "#/components/schemas/RemoteContentUploadHttpSource" }
        idempotency_key: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        purpose: { type: string, const: remote_archive }
        content_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        size_bytes: { type: integer, minimum: 1, maximum: 536870912 }
        upload_ref: { $ref: "#/components/schemas/RemoteContentUploadHttpRef" }
        state: { type: string, const: stored }
        created_at: { type: string, format: date-time }
        expires_at: { type: string, format: date-time }
        record_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    RemoteContentUploadHttpResult:
      type: object
      additionalProperties: false
      x-hunter-cross-field-invariants:
        - upload_ref exactly equals record.upload_ref
      required: [outcome, upload_ref, record]
      properties:
        outcome: { type: string, enum: [new, replay] }
        upload_ref: { $ref: "#/components/schemas/RemoteContentUploadHttpRef" }
        record: { $ref: "#/components/schemas/RemoteContentUploadHttpRecord" }
    RemoteContentUploadHttpStatus:
      type: object
      additionalProperties: false
      x-hunter-cross-field-invariants:
        - unknown is the only state with a null record
        - stored and expired have one scope-bound record
      required: [state, record]
      properties:
        state: { type: string, enum: [stored, expired, unknown] }
        record:
          oneOf:
            - { $ref: "#/components/schemas/RemoteContentUploadHttpRecord" }
            - { type: "null" }
    RemoteSyncArchiveSourceHttp:
      type: object
      additionalProperties: false
      required: [project_id, branch_name, actor_id]
      properties:
        project_id: { type: string, minLength: 1, maxLength: 160 }
        branch_name: { type: string, minLength: 1, maxLength: 160 }
        actor_id: { type: string, minLength: 1, maxLength: 160 }
        commit_sha: { type: string, minLength: 1, maxLength: 160 }
        client_id: { type: string, minLength: 1, maxLength: 160 }
        change_key: { type: string, minLength: 1, maxLength: 160 }
    RemoteSyncArchiveUploadRefHttp:
      type: object
      additionalProperties: false
      required: [ref_id, sha256, size_bytes]
      properties:
        ref_id: { type: string, minLength: 1, maxLength: 240 }
        sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        size_bytes: { type: integer, minimum: 1, maximum: 536870912 }
    RemoteSyncArchiveIdentitiesHttp:
      type: object
      additionalProperties: false
      required: [package_sha256, package_size_bytes, archive_schema_version, trusted_package_receipt_hash, local_archive_receipt_hash, manifest_hash, inventory_hash, core_v2_projection_hash]
      properties:
        package_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        package_size_bytes: { type: integer, minimum: 1, maximum: 536870912 }
        archive_schema_version: { type: integer, const: 1 }
        trusted_package_receipt_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        local_archive_receipt_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        manifest_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        inventory_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        core_v2_projection_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    RemoteSyncArchiveMetadataHttp:
      type: object
      additionalProperties: false
      required: [schema_version, source, archive_id, identities, upload_ref]
      properties:
        schema_version: { type: integer, const: 2 }
        source: { $ref: "#/components/schemas/RemoteSyncArchiveSourceHttp" }
        archive_id: { type: string, minLength: 1, maxLength: 160 }
        identities: { $ref: "#/components/schemas/RemoteSyncArchiveIdentitiesHttp" }
        upload_ref: { $ref: "#/components/schemas/RemoteSyncArchiveUploadRefHttp" }
      x-hunter-cross-field-invariants:
        - upload_ref.sha256 and upload_ref.size_bytes equal identities.package_sha256 and identities.package_size_bytes
    RemoteSyncArchiveLeaseHttp:
      type: object
      additionalProperties: false
      required: [project_id, branch_name, actor_id, capability_hash, fencing_token, acquired_at, expires_at]
      properties:
        project_id: { type: string, minLength: 1, maxLength: 160 }
        branch_name: { type: string, minLength: 1, maxLength: 160 }
        actor_id: { type: string, minLength: 1, maxLength: 160 }
        capability_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        fencing_token: { type: integer, minimum: 1 }
        acquired_at: { type: string, format: date-time }
        expires_at: { type: string, format: date-time }
      x-hunter-cross-field-invariants:
        - 0 < expires_at - acquired_at <= 600000 milliseconds
    RemoteSyncArchiveReceiptHttp:
      type: object
      additionalProperties: false
      required: [schema_version, receipt_id, operation_id, prepare_id, idempotency_key, payload_hash, source, archive_id, package_sha256, package_size_bytes, manifest_hash, trusted_package_receipt_hash, local_archive_receipt_hash, inventory_hash, core_v2_projection_hash, stored_at, receipt_hash]
      properties:
        schema_version: { type: integer, const: 2 }
        receipt_id: { type: string, pattern: "^remote_archive_receipt:sha256:[a-f0-9]{64}$" }
        operation_id: { type: string, pattern: "^remote_archive_operation:.{1,215}$" }
        prepare_id: { type: string, pattern: "^remote_archive_prepare:sha256:[a-f0-9]{64}$" }
        idempotency_key: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        payload_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        source: { $ref: "#/components/schemas/RemoteSyncArchiveSourceHttp" }
        archive_id: { type: string, minLength: 1, maxLength: 160 }
        package_sha256: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        package_size_bytes: { type: integer, minimum: 1, maximum: 536870912 }
        manifest_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        trusted_package_receipt_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        local_archive_receipt_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        inventory_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        core_v2_projection_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        stored_at: { type: string, format: date-time }
        receipt_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    RemoteSyncArchiveRecordHttp:
      type: object
      additionalProperties: false
      required: [schema_version, operation_id, prepare_id, idempotency_key, payload_hash, source, archive_id, identities, upload_ref, state, generation, lease, receipt, failure_code, created_at, updated_at, record_hash]
      properties:
        schema_version: { type: integer, const: 2 }
        operation_id: { type: string, pattern: "^remote_archive_operation:.{1,215}$" }
        prepare_id: { type: string, pattern: "^remote_archive_prepare:sha256:[a-f0-9]{64}$" }
        idempotency_key: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        payload_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        source: { $ref: "#/components/schemas/RemoteSyncArchiveSourceHttp" }
        archive_id: { type: string, minLength: 1, maxLength: 160 }
        identities: { $ref: "#/components/schemas/RemoteSyncArchiveIdentitiesHttp" }
        upload_ref: { $ref: "#/components/schemas/RemoteSyncArchiveUploadRefHttp" }
        state: { type: string, enum: [pending, prepared, committing, committed, failed] }
        generation: { type: integer, minimum: 0 }
        lease:
          oneOf: [{ $ref: "#/components/schemas/RemoteSyncArchiveLeaseHttp" }, { type: "null" }]
        receipt:
          oneOf: [{ $ref: "#/components/schemas/RemoteSyncArchiveReceiptHttp" }, { type: "null" }]
        failure_code: { type: [string, "null"] }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        record_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
      x-hunter-cross-field-invariants:
        - record_hash is the canonical record hash without record_hash
        - prepared/committing/committed records have a lease; failed records clear lease and receipt
        - committed records have a receipt and generation at least 3
    RemoteSyncArchiveClaimHttp:
      type: object
      additionalProperties: false
      required: [operation_id, prepare_id, source, generation, fencing_token, capability]
      properties:
        operation_id: { type: string, pattern: "^remote_archive_operation:.{1,215}$" }
        prepare_id: { type: string, pattern: "^remote_archive_prepare:sha256:[a-f0-9]{64}$" }
        source: { $ref: "#/components/schemas/RemoteSyncArchiveSourceHttp" }
        generation: { type: integer, minimum: 1 }
        fencing_token: { type: integer, minimum: 1 }
        capability: { type: string, pattern: "^remote_archive_capability:.{1,214}$" }
    RemoteSyncArchivePrepareHttpRequest:
      type: object
      additionalProperties: false
      required: [schema_version, operation_id, idempotency_key, payload_hash, lease_ttl_ms, metadata]
      properties:
        schema_version: { type: integer, const: 2 }
        operation_id: { type: string, pattern: "^remote_archive_operation:.{1,215}$" }
        idempotency_key: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        payload_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        lease_ttl_ms: { type: integer, minimum: 1, maximum: 600000 }
        metadata: { $ref: "#/components/schemas/RemoteSyncArchiveMetadataHttp" }
    RemoteSyncArchivePrepareHttpResponse:
      type: object
      additionalProperties: false
      required: [outcome, claim, record]
      properties:
        outcome: { type: string, enum: [new, replay] }
        claim:
          oneOf: [{ $ref: "#/components/schemas/RemoteSyncArchiveClaimHttp" }, { type: "null" }]
        record: { $ref: "#/components/schemas/RemoteSyncArchiveRecordHttp" }
      x-hunter-cross-field-invariants:
        - new has a claim; replay has null claim
    RemoteSyncArchiveCommitHttpRequest:
      type: object
      additionalProperties: false
      required: [claim, idempotency_key, payload_hash]
      properties:
        claim: { $ref: "#/components/schemas/RemoteSyncArchiveClaimHttp" }
        idempotency_key: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
        payload_hash: { type: string, pattern: "^sha256:[a-f0-9]{64}$" }
    RemoteSyncArchiveCommitHttpResponse:
      type: object
      additionalProperties: false
      required: [outcome, record, receipt]
      properties:
        outcome: { type: string, enum: [new, replay] }
        record: { $ref: "#/components/schemas/RemoteSyncArchiveRecordHttp" }
        receipt: { $ref: "#/components/schemas/RemoteSyncArchiveReceiptHttp" }
    RemoteSyncArchiveStatusHttpResponse:
      type: object
      additionalProperties: false
      required: [operation_id, state, record]
      properties:
        operation_id: { type: string, pattern: "^remote_archive_operation:.{1,215}$" }
        state: { type: string, enum: [pending, prepared, committing, committed, failed, unknown] }
        record:
          oneOf: [{ $ref: "#/components/schemas/RemoteSyncArchiveRecordHttp" }, { type: "null" }]
    RemoteSyncArchiveReceiptHttpResponse:
      type: object
      additionalProperties: false
      required: [receipt]
      properties:
        receipt:
          oneOf: [{ $ref: "#/components/schemas/RemoteSyncArchiveReceiptHttp" }, { type: "null" }]
  headers:
    RequestId:
      description: Stable request correlation identity echoed on every response.
      schema: { type: string, format: uuid }
  responses:
    Error:
      description: Standard error envelope
      headers:
        X-Request-Id: { $ref: "#/components/headers/RequestId" }
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
