// Task 11 intentionally exposes no renderer capability. Task 16 will replace
// this placeholder with a frozen, named, schema-validated IPC surface.
export const desktopPreloadCapabilities = Object.freeze([] as const);
